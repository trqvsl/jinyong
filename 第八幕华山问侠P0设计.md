# 第八幕“华山问侠”P0 设计

> 状态：P0 契约定稿，P1-P5、八类 Debug 夹具、P15 世界回响与卷末纪事均已完成。
> 依据：《射雕英雄传二创剧本大纲.md》A8-01 至 A8-05、《射雕英雄传二创游戏设定.md》第八幕长期蓝图、第七幕五类离营结果与现有剧情 / 战斗 DSL。
> 目标：用 User Motion -> Use Case -> Interface Mapping 定义真相高潮、价值高潮、武学高潮和结局矩阵，替换旧 `shendiao-huashan` 样板。

---

## 一、P0 结论

现代第八幕采用五个连续事件，不复用旧华山样板：

1. **群雄上山**：按第七幕五种 departure 决定玩家身份、同行者和可携带的见证。
2. **华山公议**：公开旧案与战争记录，决定哪些事实进入江湖公论。
3. **最后军报**：用具体行动处理伤者、记录、追责和权位冲突，完成价值高潮。
4. **绝顶论剑**：通过对决、守台、护送或观战完成武学高潮。
5. **江湖定席**：按多项前情与第八幕行动进入显式结局，不再只按 karma 自动分流。

关键工程结论：

- 不新增 `EndingScore`、`HeroismScore` 或通用结局评分器。
- 不新增通用比武大会系统；论剑继续使用现有剧情战。
- 不新增 WorldState 顶层字段；首轮继续使用 `WorldArcState.variants`。
- 现有 `Condition / Consequence / Transition` 足够表达首轮实现。
- “天下第一”是极高门槛隐藏称号，不是默认结局，也不自动等于最好结局。
- `arcBeat(shendiao/act8-huashan=done)` 只能在最终结局确认后写入。

### P2 实现结果

- `shendiao-huashan-arrival-act8` 已落地五种 departure 入场，并按同行者、NPC 生死、关系与战争结果形成 `broad / divided / sparse` 见证席。
- `shendiao-huashan-testimony-act8` 已支持旧案 / 战争两种公议顺序，两组记录均提供公开、限定、扣下与歪曲四种立场。
- `act8.record` 严格按 `falsified → concealed → full → contested` 有序规则汇总；`corrupted` 旧证据不能升级为完整记录。
- P2 只写 `act8.entry / witnesses / old-record / war-record / record / arrival / testimony`，不提前写价值、论剑、结局或幕级 beat。

### P3 实现结果

- `shendiao-huashan-crisis-act8` 已回读四类公议记录与五种入场身份，注入伤者车、追缉文书、夺卷者和公议原卷的可见冲突。
- `save-crowd / guard-record / pursue-raiders` 均使用准备 / 暴露两套剧情战编组，支持多人保护、最低幸存人数与 `partial`。
- `claim-seat` 由蒙古使者身份、声望或阵营态度开放，保留军职路线但固定写入 `trust-lost`。
- 六套剧情战的 24 个出口与直接结算路线全部写入非空 `act8.value-cost`；高前情只减少敌方编组，不清空代价。
- P3 只在最终节点写 `act8.value-stage=cleared`，不提前写论剑、结局或幕级 beat。

### P4 实现结果

- `shendiao-huashan-contest-act8` 已落地 `duel / hold-platform / protect-descent / observe / decline` 五种武学路径。
- 对决按白驼、桃花岛与郭靖关系注入三类对手；守台和护卷复用多人保护、最低幸存人数与 `partial`。
- `recognized / contender / none` 只读取武学路径、结果、声望、高阶武学与宗师 / 见证门槛，不参与结局分流。
- `shendiao-huashan-epilogue-act8` 已按 `commander → grassland → taohua → hero → keeper → hermit → outcast → wanderer` 显式排序。
- 八个最终节点同时写 `act8.ending / arcEnding / act8-huashan=done`；karma 与隐藏称号均不能单独覆盖矩阵。

### P5 80 组合审计结果

- 已覆盖 `4 record × 4 value × 5 martial-path = 80` 个唯一组合，每项独立验证重复解析确定性。
- 每个组合均验证 `recognized / contender / none` 不改变结局，`karma=-100 / 100` 不单独改写结局。
- 每个组合均进入事实节点与最终节点，并同步写入 `act8.ending / arcEnding / act8-huashan=done`，保持 `player.world === world`。
- 中立审计基线分布固定为 `keeper 3 / hermit 8 / outcast 18 / wanderer 51`。

---

## 二、设计目标与非目标

### 目标

- 让第七幕五种 departure 都有不同的华山入场身份，而不是统一重置成自由侠客。
- 把第六幕真相、第七幕战争和玩家自身责任带入华山公议。
- 让玩家通过公开、保留、歪曲或承担记录来形成真相结论，不重做铁枪庙证据小游戏。
- 用一次必须取舍的现场危机表达“何为英雄”，不让角色轮流发表主题演讲。
- 让武学强度、战斗表现和是否争名影响名望与称号，但不覆盖人物关系和价值选择。
- 让每个结局至少读取两个既往状态和一个第八幕行动。
- 保留洪七公、黄药师、欧阳锋、一灯、郭靖、黄蓉等人物自己的判断与行动。

### 非目标

- 不把华山做成可反复刷取排名和奖励的竞技场。
- 不让玩家击败一名宗师后自动成为“五绝之首”。
- 不重演第六幕四轮推理，也不重新判断杨康有罪与否。
- 不用一次最终对白覆盖前七幕行为。
- 不让正、邪、中三个结局直接对应 karma 三个区间。
- 不在 P0 编写完整事件正文或直接实现第八幕代码。
- 不在射雕卷中实现襄阳守城；襄阳只可进入远期后日谈。

---

## 三、三重高潮边界

### 1. 真相高潮：谁愿意把什么写进公论

真相高潮不再调查“发生过什么”，而是处理三件已经发生的事：

1. 铁枪庙旧案是否按证据强弱公开。
2. 桃花岛血案、幸存者和误会责任是否保留限定语。
3. 撒马尔罕军报是否包含玩家自己的军纪、撤离与屠城选择。

玩家不能靠高悟性自动得到完整记录。记录质量由此前证据和本次公开方式共同决定。

### 2. 价值高潮：当人、记录、追责与权位不能同时保住

公议进行时，最后一封战争军报抵达华山。军报携带伤者、见证人与追缉令，引发山门冲突。

玩家只能优先处理一项：

- 护送伤者和无辜见证人离开冲突区。
- 守住公议原卷与证人席。
- 追击试图夺卷或灭口的人。
- 接受权势一方的席位，要求其他人服从秩序。

此前关系可以让同伴补住一处缺口，但首轮设计至少保留一项明确代价，禁止普通路线四项全保。

### 3. 武学高潮：武功如何使用，而不只是数值多高

玩家可选择：

- 正面下场与宗师或同辈对决。
- 守住石台，连续应对多名挑战者。
- 护送证人和记录下山，以武功开路。
- 观战拆招，保留公议见证身份。
- 明确不争名次，结束军旅或门派身份。

武学结果影响名望、关系和隐藏称号，但不能单独决定结局。

---

## 四、User Motion

### UM-01 五种身份抵达华山

1. 第七幕完成后，主界面推荐华山。
2. 入山节点读取 `act7.departure`：
   - `with-guojing`：与郭靖同行，自南路入山。
   - `escort-refugees`：随伤者与见证车队抵达山脚。
   - `double-agent`：携南征密报，以假身份入山。
   - `mongol-command`：作为蒙古使者或军中代表公开到场。
   - `grassland-ending`：退出征战后应旧友之约，以草原访客身份到场。
3. 华筝、李萍、郭靖、黄蓉和蒙古阵营根据前情决定迎接、阻拦或回避。
4. 写入 `act8.entry` 与 `act8.arrival=cleared`。

### UM-02 整理公议席位与见证

1. 玩家在山腰公议棚查看三组记录：
   - 铁枪庙旧案与杨康裁决。
   - 桃花岛血案、幸存者和误会记录。
   - 武穆遗书、撒马尔罕军报与南征令。
2. 到场者由 NPC 生死、关系、阵营和 departure 决定。
3. 玩家选择先提交哪组记录，只影响质询顺序，不删除其他组。
4. 不存在“见证人越多就自动正确”；每组证言仍按原状态带限定。

### UM-03 华山公议

1. 玩家先处理旧案记录：
   - 完整公开并保留证据强弱。
   - 只公开可复核部分。
   - 扣下会伤及自己或同伴的部分。
   - 主动歪曲责任归属。
2. 玩家再处理战争记录：
   - 同时公开军令与自己的执行记录。
   - 公开军令，保留仍有争议的伤亡数字。
   - 隐去自己的服从、抗命或泄令行为。
   - 用军职、名望或门派压力改写记录。
3. 写入 `act8.old-record / act8.war-record`。
4. 汇总为 `act8.record=full / contested / concealed / falsified`。
5. 写入 `act8.testimony=cleared`。

### UM-04 最后的军报

1. 公议未结束，新的南侵军报和伤者车同时抵达。
2. 追缉者要求带走军报送达者、撒马尔罕见证人与相关原卷。
3. 玩家只能优先：
   - 护送伤者与见证人。
   - 守住公议原卷。
   - 追击夺卷者与灭口者。
   - 接受官方席位，以权力结束冲突。
4. 选择进入对应剧情战或直接结算。
5. 写入 `act8.value` 和不可为空的 `act8.value-cost`。
6. 写入 `act8.value-stage=cleared`。

### UM-05 绝顶论剑

1. 公议结果和现场代价改变可见的论剑方式。
2. 玩家选择：
   - 正面挑战一名可用对手。
   - 守台若干回合，应对多名挑战者。
   - 护送记录下山，抵挡追兵。
   - 观战悟招，不争席位。
   - 明确退出论剑。
3. 对手与友方按关系、NPC 生死和路线注入。
4. 战斗使用现有 `defeatAll / surviveRounds / protectAllyIds / partial`。
5. 写入 `act8.martial-path` 与 `act8.martial`。
6. 极高门槛时写入 `act8.title=recognized`，否则为 `contender / none`。
7. 写入 `act8.contest=cleared`。

### UM-06 江湖定席

1. 结局事件依次读取：
   - 第七幕 departure 与屠城军令结果。
   - 第六幕真相、杨康裁决与桃花岛记录。
   - 第八幕公议、价值行动与武学结果。
   - 关键人物关系、NPC 生死和阵营态度。
2. 按显式优先级选中一个结局候选。
3. 进入结局前展示触发该结局的事实，不显示隐藏分数。
4. 写入 `act8.ending` 与 `arcEnding(shendiao=...)`。
5. 最终节点写入 `arcBeat(shendiao/act8-huashan=done)`。

---

## 五、Use Case Enumeration

| ID | 用例 | 前置输入 | 玩家动作 | 可观察结果 | 持久化 |
|---|---|---|---|---|---|
| UC-01 | 确定入山身份 | `act7.departure` | 随同行者、使团或访客入山 | 入场称呼、同伴、阻拦者不同 | `act8.entry` |
| UC-02 | 组建见证席 | NPC 生死、关系、阵营、departure | 邀请旧案、桃花岛或战争见证 | 可提交记录、可用保护友方变化 | `act8.witnesses` |
| UC-03 | 公开铁枪庙旧案 | `act6.truth-strength`、杨康裁决 | 完整公开、限定公开、扣下、歪曲 | 杨康与穆念慈相关人物回应不同 | `act8.old-record` |
| UC-04 | 公开桃花岛记录 | 幸存者、误会强度、烟雨楼结论 | 接受限定、扩大战果或转移责任 | 桃花岛与江南旧人站位变化 | 并入 `act8.old-record` |
| UC-05 | 公开战争记录 | 军纪、撤离、屠城、departure | 自证、限定、隐去、改写 | 蒙古、郭靖与难民见证冲突 | `act8.war-record` |
| UC-06 | 汇总公议结论 | 两组记录结果 | 接受公议记录 | 完整、争议、隐瞒、伪造四态 | `act8.record` |
| UC-07 | 护送伤者 | 伤者与追缉者同时到场 | 守住撤离路线 | 人员保住，记录或责任人可能失去 | `act8.value=save-crowd` |
| UC-08 | 守住原卷 | 记录与证人遭抢夺 | 保护原卷和证人席 | 真相保住，部分人员受伤 | `act8.value=guard-record` |
| UC-09 | 追击责任人 | 灭口或夺卷者逃离 | 放弃原地，追击敌手 | 责任人被捕或逃脱，原地出现代价 | `act8.value=pursue-raiders` |
| UC-10 | 接受权力席位 | 军职、名望或阵营支持 | 以身份接管冲突 | 秩序恢复，证人和同伴信任受损 | `act8.value=claim-seat` |
| UC-11 | 正面论剑 | 武学、声望、可用对手 | 选择对手并对决 | 胜负、关系与称号变化 | `act8.martial-path=duel` |
| UC-12 | 守台群战 | 高声望或公议席位 | 守若干回合 | 完整、部分或失败守台 | `act8.martial-path=hold-platform` |
| UC-13 | 护卷下山 | 记录仍受威胁 | 保护证人和记录撤离 | 武学服务于真相而非名次 | `act8.martial-path=protect-descent` |
| UC-14 | 观战或退席 | 不满足挑战门槛或主动不争 | 观战、悟招、退席 | 仍可完成卷末，不锁成失败结局 | `observe / decline` |
| UC-15 | 解析结局 | 全卷状态与第八幕行动 | 确认最终去向 | 八类结局之一、人物后日谈 | `act8.ending`、arc ending |

---

## 六、前情读侧

### 第七幕入口

| 前情 | 第八幕用途 |
|---|---|
| `act7.departure=with-guojing` | 郭靖同行入山；英雄线入口最直接，但仍可在华山背离 |
| `act7.departure=escort-refugees` | 带战争见证与伤者入山；护人、守卷路线更完整 |
| `act7.departure=double-agent` | 带南征密报；公开记录会暴露军中接应 |
| `act7.departure=mongol-command` | 以官方身份到场；可调人维持秩序，也会被要求压下战争记录 |
| `act7.departure=grassland-ending` | 以草原访客身份到场；华筝与蒙古关系决定是否有使者随行 |
| `act7.order` | 决定战争记录的自证压力、蒙古追缉与郭靖态度 |
| `act7.discipline / evacuation` | 决定伤者、难民与军中证人是否能到华山 |
| `act7.huazheng` | 决定草原书信、使者、阻拦或缺席版本 |
| `act7.liping` | 决定李萍本人、口信或遗留军令是否进入公议 |

### 第六幕与旧债

| 前情 | 第八幕用途 |
|---|---|
| `act6.truth-strength` | 旧案记录能否完整公开，还是必须标记争议 |
| `act6.yangkang-verdict` | 杨康本人、审押记录、穆念慈或遗物以不同形式到场 |
| `act6.munianci-outcome` | 穆念慈是否愿意出席或只提交书面证词 |
| `act6.island-outcome` | 桃花岛幸存者与血案原始记录是否存在 |
| `act6.misunderstanding / yanyu-outcome` | 桃花岛责任是否已经得到足够旁证 |
| `act5.wumu-destination` | 战争记录由宋军、丐帮、蒙古或玩家哪一方持有 |
| `act5.ouyangke` | 白驼山见证、欧阳锋站位和可用论剑对手 |
| `act4.taohua-route` | 桃花岛关系者、黄药师与黄蓉的入场版本 |
| 裘千仞、一灯、周伯通、瑛姑状态 | 决定旧罪、赎罪与南帝一极是否进入公议 |

### 角色与数值

- 郭靖、黄蓉、黄药师、洪七公、华筝、穆念慈关系决定谁愿意替玩家补位。
- 蒙古态度决定官方使团、追缉与军职席位。
- `hasSkill`、声望和等级只控制特殊挑战或演出，不控制道德结局。
- karma 只允许作为结局同条件下的最后 tie-breaker，不得作为第一判定项。

---

## 七、状态契约

### 阶段状态

| Key | Value |
|---|---|
| `act8.arrival` | `cleared` |
| `act8.testimony` | `cleared` |
| `act8.value-stage` | `cleared` |
| `act8.contest` | `cleared` |

### 结果状态

| Key | 建议值 |
|---|---|
| `act8.entry` | `guo-company / refugee-witness / covert-messenger / mongol-envoy / grassland-visitor` |
| `act8.witnesses` | `broad / divided / sparse` |
| `act8.old-record` | `disclosed / qualified / withheld / distorted` |
| `act8.war-record` | `disclosed / qualified / withheld / distorted` |
| `act8.record` | `full / contested / concealed / falsified` |
| `act8.value` | `save-crowd / guard-record / pursue-raiders / claim-seat` |
| `act8.value-cost` | `record-damaged / people-hurt / culprit-escaped / trust-lost` |
| `act8.martial-path` | `duel / hold-platform / protect-descent / observe / decline` |
| `act8.martial` | `won / held / partial / lost / understood / refused` |
| `act8.title` | `recognized / contender / none` |
| `act8.ending` | `hero / taohua / grassland / commander / keeper / hermit / outcast / wanderer` |

### 幕级完成

- `arcBeat(shendiao/act8-huashan=done)` 只在结局演出完成后写入。
- 同一节点写入 `arcEnding(shendiao=<act8.ending>)`。
- 现代流程不写旧 `huashan` beat。
- 旧存档 `act7-western-campaign=skipped` 仍可触发旧 `shendiao-huashan` 样板。

---

## 八、真相汇总规则

`act8.record` 不使用分数，按顺序判定：

1. 任一记录选择 `distorted`：`falsified`。
2. 无歪曲，但任一记录选择 `withheld`：`concealed`。
3. 两组记录都为 `disclosed`，且基础证据没有 `corrupted`：`full`。
4. 其他组合：`contested`。

约束：

- `act6.truth-strength=corrupted` 时，即使玩家选择公开，也只能得到 `contested`，不能把争议信息写成完整事实。
- `act7.order=obeyed` 不锁死公开选项；玩家可以公开自己的执行记录，但必须承担关系和军职后果。
- `act7.order=defied / betrayed` 不自动获得完整记录；证人、军报和玩家陈述仍需互证。
- `falsified` 是主动改写，不等同证据不足。

---

## 九、价值行动与必付代价

| `act8.value` | 玩家优先保护 | 默认代价 | 可被前情缓解 |
|---|---|---|---|
| `save-crowd` | 伤者、难民与见证人 | `record-damaged` 或 `culprit-escaped` | 黄蓉、丐帮、郭靖可代守一项 |
| `guard-record` | 原卷、证物与公议席 | `people-hurt` | 完整撤离与高关系同伴可减少伤者 |
| `pursue-raiders` | 夺卷者、灭口者与责任人 | `people-hurt` 或 `record-damaged` | 斥候 / 骑术前情可缩短追击 |
| `claim-seat` | 军令、门派或名望秩序 | `trust-lost` | 高阵营态度只改变支持者，不消除代价 |

验收要求：

- `act8.value-cost` 不得为空。
- 普通路线不能同时保住人、记录、责任人和权位。
- 极高前情只允许同伴补住一项，不允许把代价全部清零。
- 价值行动必须发生在可见人物与物件上，不写抽象“侠义值”。

---

## 十、武学高潮契约

### 论剑路径

| 路径 | 战斗表达 | 主要读侧 |
|---|---|---|
| `duel` | `defeatAll` 单体或分阶段对决 | 武学、声望、对手关系 |
| `hold-platform` | 多敌方 `surviveRounds` | 声望、公议席位、战争经历 |
| `protect-descent` | `surviveRounds + protectAllyIds + partial` | 见证人、记录、价值选择 |
| `observe` | 无战斗，按已学武功给不同拆招文本 | `hasSkill`、悟性、宗师关系 |
| `decline` | 无战斗，明确退出名次争夺 | value 结果、人物关系 |

### 隐藏称号

`act8.title=recognized` 至少需要：

1. 选择 `duel` 或 `hold-platform`。
2. 达成对应 `won`。
3. 声望和武学门槛满足。
4. 至少一名宗师关系或见证成立。

称号只影响名望、后日谈与部分结局文本：

- 不自动覆盖 `act8.ending`。
- 不把 `hero` 变成默认最好结局。
- `observe / decline` 仍能进入完整人物结局。

---

## 十一、结局矩阵

结局使用有序 `branch`，每个非兜底结局必须满足至少两个既往状态和一个第八幕行动。

| 优先级 | ending | 最低条件方向 | 明确排除 |
|---|---|---|---|
| 1 | `commander` | `departure=mongol-command` + 蒙古态度高 + `value=claim-seat/pursue-raiders` | 公开放弃军职或转护证人 |
| 2 | `grassland` | 草原 departure / 华筝高关系 + record 非 falsified + `save-crowd/guard-record` | 主动夺取中原权位 |
| 3 | `taohua` | 桃花岛前情或黄蓉 / 黄药师高关系 + record 完整/争议 + `guard-record` | 蒙古军职未解除 |
| 4 | `hero` | 郭靖同行或护难民 + 屠城未服从 + record 非伪造 + `save-crowd/guard-record` | 郭靖关系决裂 |
| 5 | `keeper` | 护难民 / 双面人 + `record=full` + `guard-record` + 不争或护卷 | 主动改写记录 |
| 6 | `hermit` | `save-crowd` + `observe/decline` + 至少一名存活同伴关系高 | 仍保留征战军职 |
| 7 | `outcast` | `record=falsified`，或夺位且主要关系崩溃 | 已满足更高优先级权力结局 |
| 8 | `wanderer` | 其他合法组合 | 无 |

补充规则：

- `commander` 是权力与草原军职结局，不等同通用邪道结局。
- `grassland` 读取华筝自主选择，不把她作为玩家奖励。
- `taohua` 必须读取桃花岛前情，不能只靠黄蓉单次好感。
- `hero` 必须包含第八幕具体护人或守证行动。
- `keeper` 强调保存记录和见证，不要求玩家赢得论剑。
- `hermit` 不是失败结局，必须有主动退出名次与安置他人的行动。
- `outcast` 需要主动伪造或权力行为加关系崩溃，不能只因 karma 低自动进入。
- `wanderer` 是合法兜底，不写成“什么都没做到”。

---

## 十二、事件与地点拆分

### 地点

首轮只复用 `huashan`，不新增地图地点。

华山同一地点通过连续事件和动态路引区分：

- 山脚来路与见证车队。
- 山腰公议棚。
- 下峰驿路与最后军报。
- 绝顶石台。
- 结局离山路线。

### 事件

| 顺序 | eventId | 主要职责 |
|---|---|---|
| 1 | `shendiao-huashan-arrival-act8` | 五种 departure 入场、同行者与见证席 |
| 2 | `shendiao-huashan-testimony-act8` | 旧案、桃花岛、战争记录与真相汇总 |
| 3 | `shendiao-huashan-crisis-act8` | 最后军报、现场取舍与价值高潮 |
| 4 | `shendiao-huashan-contest-act8` | 对决、守台、护卷、观战或退席 |
| 5 | `shendiao-huashan-epilogue-act8` | 有序结局分流、后日谈与幕级完成 |

### 地图事件优先级

`huashan.events` 应按后段优先：

1. `shendiao-huashan-epilogue-act8`
2. `shendiao-huashan-contest-act8`
3. `shendiao-huashan-crisis-act8`
4. `shendiao-huashan-testimony-act8`
5. `shendiao-huashan-arrival-act8`
6. 旧 `shendiao-huashan`
7. 旧华山余波与通用事件

### 动态路引

1. 第七幕完成且 `act8.arrival` 未完成：启程赴华山。
2. `act8.arrival=cleared`：参加山腰公议。
3. `act8.testimony=cleared`：处理最后军报。
4. `act8.value-stage=cleared`：登绝顶论剑。
5. `act8.contest=cleared`：确认江湖去向。
6. `act8-huashan=done`：射雕卷完成态。

---

## 十三、Interface Mapping

### 1. 剧情 DSL

现有能力足够：

| 需求 | 现有表达 |
|---|---|
| 五种入场身份 | `arcVariant act7.departure` + `branch` |
| 见证人到场 | NPC 生死、relation、faction、`and/or/not` |
| 两组记录选择 | Choice consequences 写 `act8.old-record / war-record` |
| 真相汇总 | 有序 `branch` 写 `act8.record` |
| 价值行动与代价 | Choice + battle outcome 写 `value / value-cost` |
| 多种论剑方式 | `battle / goto / branch` |
| 显式结局矩阵 | 有序 `branch` + `arcEnding` |
| 最终完成 | 最后节点 `arcBeat act8-huashan=done` |

不新增：

- 数值型 ending score。
- mastery 条件；首轮使用 `hasSkill / reputation / relation` 控制特殊挑战。
- 通用公议系统。
- 通用赛事或排行榜系统。

### 2. 战斗映射

- 正面对决：`defeatAll`。
- 守台：`surviveRounds`，可配置多敌方。
- 护卷下山：`surviveRounds + protectAllyIds + minProtectedSurvivors + onPartial`。
- 最后军报冲突：按 value 选择注入不同敌人、友方与目标。
- `won / partial / lost / fled` 均必须有结局读侧。

### 3. 数据层

- 新建 `src/data/story/shendiaoAct8.ts`，不扩张 `shendiao.ts` 旧样板段。
- `src/data/story/index.ts` 聚合现代第八幕。
- `src/data/map.ts` 调整华山事件优先级。
- `src/data/story/progress.ts` 增加四段幕内路引覆盖。
- `src/data/story/debugPresets.ts` 增加第八幕入口和典型结局夹具。
- 旧 `shendiao-huashan` 继续只服务 legacy `act7=skipped` 存档。

### 4. 界面

- `EventScreen` 继续使用单人单拍、选择、autoNext 和 result。
- 不新增聊天气泡、公议表格或结局分数面板。
- 结局前可显示“触发事实”列表，但必须来自明确状态，不显示隐藏权重。
- `BattleScreen` 复用现有目标与保护组 UI。

---

## 十四、旧样板兼容

现代与旧华山必须互斥：

- 现代五事件要求 `act7-western-campaign=done`。
- 旧 `shendiao-huashan` 要求 legacy `yangkang` beat，且 `act7-western-campaign` 不是 `done`。
- 旧迁移产生 `act7-western-campaign=skipped` 时，仍允许旧样板完成兼容流程。
- 现代第八幕只写 `act8-huashan`，不写旧 `huashan` beat。
- 旧 `shendiao-huashan-snow / stone` 不得抢占现代事件。

---

## 十五、验收矩阵

### 结构验收

- 五个现代事件都能从入口到终点。
- 华山地图按 arrival -> testimony -> crisis -> contest -> epilogue 顺序露出。
- `act8-huashan` 只在最终结局节点写入。
- 现代与旧华山事件互斥，legacy skipped 兼容保留。

### 入口验收

- 五种 `act7.departure` 都能进入现代第八幕。
- 每种入口至少改变同行者、见证人或可用选项中的两项。
- `mongol-command` 不得被强制改写成叛将。
- `grassland-ending` 可以赴约上山，但不自动失去草原结局候选。

### 真相验收

- `full / contested / concealed / falsified` 四态都可达。
- 证据 `corrupted` 时不能得到 `full`。
- 主动伪造与证据不足必须是不同状态。
- 玩家公开自身战争责任时，关系与军职后果必须立即可见。

### 价值验收

- 四种 value 行动都能完成事件。
- `act8.value-cost` 在所有路线中非空。
- 普通路线不能四项全保。
- 价值高潮必须以行动和具体人物呈现，不使用单次问答决定结局。

### 武学验收

- `duel / hold-platform / protect-descent / observe / decline` 都能完成本幕。
- 不参加战斗不等于失败结局。
- `partial` 在护卷或守台路线有独立后果。
- `act8.title=recognized` 不能覆盖结局矩阵。

### 结局验收

- 八类 ending 都存在至少一个可达夹具。
- 每个非兜底 ending 至少读取两个既往状态和一个第八幕行动。
- karma 不得单独决定 ending。
- 同一状态组合只命中一个有序分支。
- 建议覆盖 `4 record x 4 value x 5 martial-path = 80` 组合的确定性审计。

### 叙事验收

- 公议不重演铁枪庙长篇推理。
- 洪七公不承担主题总结独白。
- 欧阳锋武学压制与身份错乱的反讽通过行动呈现。
- 华筝、李萍、穆念慈、黄蓉、黄药师等人物结论继续保持自主性。
- 不替玩家写“终于明白何为侠义”。

---

## 十六、后续实施顺序

1. P1（已完成）：新建 `shendiaoAct8.ts`，接入五事件 id、地图优先级、聚合入口、动态路引与第八幕 Debug 入口；P2 前不注册空壳事件。
2. P2（已完成）：落地五种入场身份、三档见证席、旧案 / 战争记录公议与四态汇总。
3. P3（已完成）：落地最后军报、四种价值行动、六套局部剧情战与必付代价。
4. P4（已完成）：落地绝顶论剑、隐藏称号、八类结局与卷末 beat。
5. P5（进行中）：80 组合结局审计已完成；待补八类结局夹具、旧样板隔离与浏览器验证。

# 待做事项

> 说明：这里作为后续迭代的统一待办清单。  
> 新需求归纳后写入本文件；完成一项后，将其改为 `- [x] ~~事项~~` 形式划掉。

---

## 高优先级

- [x] ~~修复剧情系统 `seenNodes` 仅按裸 `nodeId` 去重的问题，改为带事件作用域的去重键，避免不同事件之间互相污染 `onEnter`。~~
- [x] ~~将 `createPlayer()` 中的测试强化版初始值与正式体验隔离，避免继续污染战斗、奖励与经济节奏判断。~~

## 中优先级

- [x] ~~继续瘦身 `App.tsx`，把更多剧情 / 战斗 / 主界面回流编排下沉到 `src/game/appFlow.ts` 或更清晰的编排层。~~
- [x] ~~为剧情系统补充静态结构校验：如 `node key === node.id`、`goto` 目标存在、`gotoEvent` 存在、危险重复 `nodeId` 警告等。~~
- [x] ~~继续坚持“结构化字段优先于 `flags`”的扩展原则，减少未来再次滑回 flag 堆叠。~~
- [x] ~~进一步统一 battle 模块的公共边界，明确 `engine / adapter / screen` 三层职责，避免后续继续混杂。~~
- [x] ~~顺着 battle 边界继续抽离 `BattleScreen` 中的 battle flow/runtime helper（ATB 调度、支援落地、战后收尾）。~~

## 低优先级

- [x] ~~在 README 或 CLAUDE 索引中补更清晰的阅读路线，方便以后快速定位文档。~~
- [ ] 明确并落地“事件内进度是否持久化”的策略；若暂不实现，也需在系统与文档中统一约定中途退出后的行为。
- [x] ~~补一份轻量“内容编写 checklist”，统一 event id、node id、relationType、arcBeat、letter presentation 等使用规范。~~
- [ ] 后续为 Debug / 调试能力补更强的 world 可视化，便于查看 `npcs / factions / arcs / flags / party / pendingWorldEvents`。

---

## 备注

当前这份清单首版来自“方案 C：优先优化建议”的整理结果。后续所有迭代，都以此文件作为统一追加与勾销入口。
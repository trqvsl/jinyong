# 剧情系统设计手册

> 金庸群侠传的灵魂不是“有很多剧情”，而是**选择会留下后果，世界会记住你做过什么**。  
> 本手册描述的是**当前代码已经落地的剧情系统结构与运行方式**，不是纯概念设计稿。若实现继续演进，请同步更新本文。

---

## 一、核心理念：选择塑造世界

剧情系统的核心不是“节点跳转图”，而是：

> **所有剧情都读写同一份 WorldState，节点之间通过世界状态发生因果。**

玩家的一次选择，会沿四层传播：

| 影响层次 | 例子 | 落点 |
|---|---|---|
| ① 自身收益 / 属性 | 得银两、受伤、习武功、声望涨跌 | `player` |
| ② NPC 好感 | 丘处机好感 +15 | `player.relations` |
| ③ NPC 命运 / 阵营 / 关系性质 | 杨康黑化、NPC 入队、阵营态度变化、关系从“初识”变“朋友” | `player.world.npcs` / `player.world.factions` |
| ④ 后续剧情 / 世界回响 | 满足 karma 或 relation 条件后浮现特殊事件 | `Condition` + `WorldState` |

所以，剧情系统的基本单位不是“文案”，而是：

- **声明式写入**：`Consequence`
- **声明式查询**：`Condition`
- **声明式流转**：`Transition`

---

## 二、当前代码结构与职责

```text
src/data/story/
  schema.ts          剧情 DSL 与 WorldState 类型
  shendiao.ts        射雕卷剧情数据
  worldEvents.ts     世界回响事件数据
  index.ts           剧情卷聚合

src/data/events.ts   通用奇遇事件

src/game/story/
  state.ts           WorldState 初始化 / 默认值 / 迁移
  conditions.ts      Condition 解释器
  consequences.ts    Consequence 解释器
  engine.ts          进入节点 / 结算选项 / branch / 战斗结果衔接
  query.ts           地点剧情查询 / 随机通用事件兜底
  worldScheduler.ts  世界回响调度器

src/game/appFlow.ts  剧情 / 战斗 / 主界面回流编排
src/screens/EventScreen.tsx  剧情展示层
src/App.tsx          全局路由与 screen 编排
```

### 分层原则

- `src/data/story/*`：只写剧情内容，不写流程逻辑
- `src/game/story/*`：只解释 schema，不直接认识具体剧情文案
- `EventScreen`：只做表现与交互，不自己决定剧情规则
- `App.tsx` / `appFlow.ts`：负责根据 `Transition` 做总编排

---

## 三、WorldState：当前真实结构

`WorldState` 定义在 `src/data/story/schema.ts`，并挂在 `player.world` 上随存档保存。

```ts
interface WorldState {
  version: number
  npcs: Record<string, WorldNpcState>
  factions: Record<string, WorldFactionState>
  arcs: Record<string, WorldArcState>
  flags: Record<string, boolean | number | string>
  party: {
    activeNpcIds: string[]
    reserveNpcIds: string[]
  }
  pendingWorldEvents: string[]
  triggeredEvents: string[]
  seenNodes: string[]
  completedEvents: string[]
  currentStory: StoryCheckpoint | null
}
```

### 1. `npcs`
记录 NPC 的命运态与关系语义，而不是单纯好感值：

- `alive`
- `recruited`
- `faction`
- `fateTags`
- `relationType?`

其中 **好感数值** 仍保留在 `player.relations`，与 `relationType` 正交。

### 2. `factions`
记录阵营对玩家的态度与势力变化：

- `attitude`
- `power`

### 3. `arcs`
记录主线 / 事件链的 beat 进度：

- `won`
- `lost`
- `skipped`
- `done`

### 4. `flags`
扁平兜底标记，用于无法自然归入结构化字段的细小状态。

### 5. `party`
记录当前队伍状态：

- `activeNpcIds`
- `reserveNpcIds`

这是队伍 / 羁绊 / 战斗支援系统进入世界存档后的正式字段。

### 6. `pendingWorldEvents`
待查看的江湖消息 / 世界回响事件队列。  
世界事件不是命中后立刻硬切屏，而是先入这个队列，再由主界面打开。

### 7. `triggeredEvents`
已触发过的世界回响 id，用于防止重复触发。

### 8. `seenNodes`
记录已经执行过 `onEnter` 的事件作用域节点键，用来避免重复结算。

> **当前实现口径**：`seenNodes` 里记录的是 **`${eventId}:${nodeId}`**，不是裸 `nodeId`。也就是说，不同事件即便复用了同名节点（如 `main` / `result` / `arrival`），它们的 `onEnter` 也会分别按所属事件独立结算，不再互相污染。

### 9. `completedEvents`
记录已经完成的 `once: true` 事件 id，完成后不再触发。

### 10. `currentStory`
记录当前事件断点，包含：

- `eventId`
- `nodeId`
- `phase`（`choosing / autoNext / result / battle`）
- `pageIndex`
- `locationId`
- 结果页待处理的 `transition / consumedDay`
- 剧情战恢复所需的敌人 id

进入事件时立即建立断点；翻页、选择、进入战斗和战后结果都会更新。事件正常结束或进入结局后清空。

刷新后读取存档，会恢复到原节点与页码。若刷新发生在剧情战中，当前口径是**从该场战斗开场恢复**，不保存战斗回合内的瞬时状态。

---

## 四、默认值与迁移

实现位于 `src/game/story/state.ts`。

### 1. 默认值读取
当 `world` 中某个 NPC / faction 还没有条目时，查询按默认值处理：

- `npc.alive` → `true`
- `npc.recruited` → `false`
- `npc.faction` → `""`
- `npc.fateTags` → `[]`
- `faction.attitude` → `0`
- `faction.power` → `0`
- `arcBeat` → 未完成
- `relationType` 缺省视作“初识”语义

这保证了 `Condition` 在“尚未写入 world”的情况下也能稳定运行。

### 2. 存档迁移
`migrateWorld()` 会在读取旧存档时补齐新字段。

当前已经兼容过的结构演进包括：

- `relationType` 加入 `WorldNpcState`
- `pendingWorldEventId` 迁移为 `pendingWorldEvents` 队列
- `party` 字段加入 `WorldState`
- 旧射雕事件到 `arcBeat` 的兼容补写
- `currentStory` 事件断点
- 旧射雕 8 节点 beat 到八幕里程碑的兼容映射

所以：

> **以后只要加 WorldState 字段，必须同步修改 `migrateWorld()`。**

### 3. 射雕八幕进度契约

幕级数据定义在 `src/data/story/progress.ts`，查询解释位于 `src/game/story/query.ts`。

新主线只使用以下 beat 表示幕级完成状态：

- `act1-wind`
- `act2-damos`
- `act3-zhongdu`
- `act4-taohua`
- `act5-old-debts`
- `act6-truth`
- `act7-western-campaign`
- `act8-huashan`

人物生死、关系性质、阵营变化与路线版本分别使用 `npcAlive / npcTag / relationType / faction`；只有无法自然归类的细节才使用 `flag`。

旧线性样板仍写入 `niujia / damos / wangfu / taohua / yangkang / huashan` 等 beat。查询层会即时解释这些旧值，迁移层也会补写对应幕级 beat；其中旧样板没有实现的第五幕和第七幕会明确记为 `skipped`，不伪装成已经完成的新内容。

---

## 五、Consequence：声明式写入

定义在 `src/data/story/schema.ts`，解释器在 `src/game/story/consequences.ts`。

当前已支持的后果分四类：

### 1. 玩家自身收益 / 属性
- `reputation`
- `karma`
- `gold`
- `exp`
- `hp`
- `mp`
- `aptitude`
- `attack`
- `speed`
- `item`
- `skill`

### 2. NPC 好感
- `relation`

### 3. NPC / 阵营命运
- `npcAlive`
- `npcRecruit`
- `npcFaction`
- `npcRelationType`
- `npcTag`
- `factionAttitude`
- `factionPower`

### 4. 主线推进 / 卷结局 / 兜底标记
- `arcBeat`
- `arcEnding`
- `flag`

### 实现特征

#### 数值支持 `delta` / `set`
例如：

- `{ kind: "karma", delta: 5 }`
- `{ kind: "gold", set: 0 }`

#### 有界字段会 clamp
在 `applyConsequences()` 中：

- `karma`、`relation`、`factionAttitude` 会夹到 `[-100, 100]`
- `hp`、`mp` 会夹到 `[0, max]`

#### `karma` 改变后自动重算 `alignment`
`alignment` 不是直接剧情写入的主字段，而是由 `karma` 派生。

#### 关键同步约束
`applyConsequences()` 最后会执行：

```ts
p.world = w
```

这是剧情系统的关键约束：

> **world 上的变化必须同步写回 `player.world`，否则 arcBeat / arcEnding / flags / NPC 命运会丢失。**

#### `flag` 的使用原则
`flag` 仍然保留，但它是**兜底字段**，不是默认扩展点。

新增剧情状态时，优先考虑是否应落到现有结构化字段：

- 主线推进 / 作品卷结果 → `arcBeat` / `arcEnding`
- NPC 关系语义 → `npcRelationType`
- NPC 命运标签 → `npcTag`
- NPC 生死 / 是否入队 / 所属阵营 → `npcAlive` / `npcRecruit` / `npcFaction`
- 阵营变化 → `factionAttitude` / `factionPower`
- 一次性触发记录 → `completedEvents` / `triggeredEvents`

只有当前确实没有合适结构化槽位，且后续确有读侧需求时，才应继续新增 `flag`。

---

## 六、Condition：声明式查询

定义在 `schema.ts`，解释器在 `src/game/story/conditions.ts`。

当前支持：

- `reputation`
- `karma`
- `relation`
- `npcAlive`
- `npcRecruited`
- `npcHasTag`
- `npcRelationType`
- `factionAttitude`
- `arcBeat`
- `flag`
- `hasItem`
- `hasSkill`
- `and`
- `or`
- `not`

这套查询既用于：

- 事件是否可触发
- 节点 / 选项是否可见
- `branch` 自动分支
- 世界回响触发条件

---

## 七、Transition：声明式流转

定义在 `schema.ts`，路由主要由 `engine.ts` + `appFlow.ts` 处理。

当前支持：

- `end`
- `goto`
- `branch`
- `random`
- `battle`
- `gotoEvent`
- `gameOver`

### 1. `goto`
在同一事件内跳到另一个节点。

### 2. `branch`
按当前世界状态自动分流。  
`resolveBranch()` 会递归把 `branch` 解开，直到得到实际应走的非 branch transition。

### 3. `random`
加权随机分流。  
`pickRandom()` 负责真正抽签，`branch` 本身不带随机副作用，而 `random` 带。

### 4. `battle`
剧情战斗桥接点，支持：

- 指定敌人 `enemyId`
- 地点敌人池 `useLocationPool`
- 致命战斗 `lethal`
- `onWin` / `onLose` / `onFlee`

### 5. `gotoEvent`
跨事件跳转，用于一段剧情结束后切入另一个事件。

### 6. `gameOver`
坏结局 / 致命结局占位。

---

## 八、节点 / 事件：当前真实运行方式

### 1. `StoryNode`
节点支持几种形态：

- **选择节点**：有 `choices`
- **纯叙事节点**：无 `choices`，但有 `autoNext`
- **条件分叉节点**：`autoNext` 是 `branch`
- **终点节点**：两者都无

### 2. `enterNode()`
位于 `src/game/story/engine.ts`。

进入节点时：

1. 找到 `event.nodes[nodeId]`
2. 生成事件作用域键 ``${event.id}:${nodeId}``
3. 若该 scoped key 不在 `seenNodes` 中，则首次执行 `onEnter`
4. 执行后把 scoped key 加入 `seenNodes`
5. 返回 node + 新 player/world

### 3. `visibleChoices()`
过滤掉 condition 不满足的选项。

### 4. `resolveChoice()`
执行选项 consequences，返回：

- 新的 `player`
- 新的 `world`
- `resultText`
- `transition`

---

## 九、地点剧情查询机制

实现位于 `src/game/story/query.ts`。

这是当前剧情露出的一个关键规则：

```text
进入地点
  ↓
读取该地点 events 数组
  ↓
从前往后找第一个“未完成 + 条件满足”的事件
  ↓
若找到，直接返回它
  ↓
若都找不到，则回退到通用 STORY_EVENTS 随机奇遇
```

所以：

> **地点 `events` 数组顺序本身就是优先级。**

这就是为什么主线事件需要排在通用事件前面，否则会被通用事件抢走。

---

## 十、世界回响（World Events）真实流程

数据在 `src/data/story/worldEvents.ts`，调度在 `src/game/story/worldScheduler.ts`，主界面编排在 `src/game/appFlow.ts`。

### 当前实现流程

```text
玩家回到主界面
  ↓
normalizeMainPlayer()
  ↓
pollWorldEvent(player, world)
  ↓
扫描 WORLD_EVENTS，找第一个满足 trigger 的 world event
  ↓
若 once=true，则先写入 triggeredEvents
  ↓
把命中的 event.id 入 pendingWorldEvents 队列
  ↓
MainScreen 展示“待处理江湖消息”
  ↓
玩家点击后才进入 EventScreen
```

### 关键特征

- 每次回主界面**最多只入队一个** world event
- `triggeredEvents` 负责 once 世界事件去重
- 世界回响不是强制插脸弹窗，而是变成主界面里的待处理消息

这使世界回响更像“江湖消息 / 来信 / 夜访 / 密帖”，而不是简单的打断式事件。

---

## 十一、EventScreen：展示层已经具备的能力

实现位于 `src/screens/EventScreen.tsx`。

当前 EventScreen 不只是“显示一段文案 + 几个按钮”，而是已经形成了一个轻量剧情阅读器。

### 1. 三种 phase

- `choosing`：读剧情并做选择
- `autoNext`：纯叙事节点，看完后继续
- `result`：展示结果文本，再按 transition 继续

### 2. 分页阅读
长文本会拆成多页，玩家轻触翻页，而不是整段直接砸出来。

### 3. 旁白 / 对话拆解
通过 `eventPresenter` 系列函数，把文案拆成：

- narration
- dialogue

并给对白单独样式。

### 4. `letter` presentation
世界回响可用书信样式展示，支持：

- `presentation: "letter"`
- `letterStyle`
- `letterIntro`
- `letterSignature`

这已经是当前剧情系统体验层的重要能力，不再只是数据结构层功能。

---

## 十二、剧情与战斗的衔接

### 1. 剧情进入战斗
`resolveStoryFlow()` 会把 `battle` transition 转成：

- 敌人列表
- 待处理 battle transition
- 进入 `BattleScreen`

### 2. 战斗返回剧情
`resolveBattleFlow()` 根据：

- `won`
- `lost`
- `fled`

去调用 `resolveBattleOutcome()`，得到：

- 结果文本
- consequences
- 后续 `then` transition

之后再回到 `EventScreen` 的 `result` 阶段。

### 3. 致命战斗
当 `battle.lethal === true` 且战败时，会走 `gameOver` 结果。

---

## 十三、当前已知约束 / 坑

### 1. 剧情战只恢复到开场
`currentStory` 会保存剧情战的敌人和后续 transition，但不会序列化 ATB、当前回合、单位瞬时气血与动画状态。
因此刷新发生在剧情战中时，会从该场战斗开场重新开始；选择后果与进入战斗前已经结算的状态不会重复执行。

### 2. 地点剧情优先级强依赖数组顺序
`map.ts` 中 `events` 的排列顺序决定谁先露出，后续内容越多越要小心。

### 3. world event 每轮只入队一个
这是当前稳定性的好处，但也意味着同一轮满足多个世界回响时，后面的要等下一次回主界面。

### 4. `flag` 仍然是兜底字段
它很有用，但如果过度依赖，后续会重新滑回“flag 堆剧情”。优先使用结构化字段。

---

## 十四、关键文件速查

### 数据层
- `src/data/story/schema.ts`
- `src/data/story/progress.ts`
- `src/data/story/shendiao.ts`
- `src/data/story/worldEvents.ts`
- `src/data/events.ts`
- `src/data/map.ts`

### 引擎层
- `src/game/story/state.ts`
- `src/game/story/conditions.ts`
- `src/game/story/consequences.ts`
- `src/game/story/engine.ts`
- `src/game/story/query.ts`
- `src/game/story/worldScheduler.ts`
- `src/game/appFlow.ts`

### 展示层
- `src/screens/EventScreen.tsx`
- `src/screens/eventPresenter.ts`

---

## 十五、结论

当前这套剧情系统已经不是“事件弹窗系统”，而是一套具备以下能力的轻量 CRPG 剧情框架：

- 共享世界状态
- 声明式条件 / 后果 / 流转
- 地点剧情优先 + 通用奇遇兜底
- 世界回响队列
- 剧情战斗衔接
- 书信、分页、对白分层的展示能力

它最重要的价值在于：

> **内容可以继续扩，但扩写所依赖的底层契约已经基本立住。**

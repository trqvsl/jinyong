# 金庸群侠传 · 项目架构导览（方案 A）

> 目的：把当前项目的真实架构、模块边界与主流程讲清楚，供 review 使用。  
> 依据：以当前 `src/` 代码实现为准，而不是历史文档或早期设计稿。

---

## 一、整体判断

这个项目已经不是“页面 + 几个功能”的拼装 demo，而是一个具有明确分层的前端单机 RPG 骨架。

当前最核心的结构是四层：

```text
src/data      负责“世界是什么”
src/game      负责“系统怎么运行”
src/screens   负责“怎么展示给玩家”
src/App.tsx   负责“当前该进哪个系统 / 哪个 screen”
```

这套结构使项目可以持续扩写剧情、战斗、NPC 与队伍系统，而不至于所有逻辑都堆进页面组件。

---

## 二、四层分别负责什么

## 1. `src/data/`：内容与静态定义层

这里主要放：

- 地图
- 敌人
- 技能
- NPC
- 门派
- 道具
- 剧情事件
- 世界回响事件

关键文件示例：

- `src/data/map.ts`
- `src/data/events.ts`
- `src/data/story/schema.ts`
- `src/data/story/shendiao.ts`
- `src/data/story/worldEvents.ts`
- `src/data/npcs.ts`

这层的职责是：

> 尽量描述“有哪些东西”，而不是“这些东西怎么运行”。

---

## 2. `src/game/`：规则引擎层

这是项目真正的核心。

### 剧情引擎

关键文件：

- `src/game/story/state.ts`
- `src/game/story/conditions.ts`
- `src/game/story/consequences.ts`
- `src/game/story/engine.ts`
- `src/game/story/query.ts`
- `src/game/story/worldScheduler.ts`

负责：

- 世界状态默认值与迁移
- 条件判断
- 后果写入
- 节点进入与选项结算
- 地点事件查询
- 世界回响调度

### 战斗引擎

关键文件：

- `src/game/battle/types.ts`
- `src/game/battle/engine.ts`
- `src/game/battle/adapter.ts`

负责：

- 战斗内部类型建模
- 伤害结算
- 状态系统
- 目标解析
- ATB / CTB 行动推进
- 多对多战斗

### 角色 / 数值 / 队伍

关键文件：

- `src/game/attributes.ts`
- `src/game/player.ts`
- `src/game/party.ts`
- `src/game/npc.ts`
- `src/game/relations.ts`
- `src/game/appFlow.ts`

负责：

- 根基属性推导
- 角色创建与存档迁移
- 队伍 / 羁绊 / 支援
- NPC 交互桥接
- 剧情 / 战斗 / 主界面的总流程编排

---

## 3. `src/screens/`：表现与交互层

这里不是定义规则的地方，而是把当前状态展示出来。

关键文件：

- `src/screens/EventScreen.tsx`
- `src/screens/BattleScreen.tsx`
- `src/screens/MainScreen.tsx`
- `src/screens/MapScreen.tsx`
- `src/screens/NpcScreen.tsx`
- `src/screens/CharacterScreen.tsx`
- `src/screens/SectScreen.tsx`
- `src/screens/ShopScreen.tsx`
- `src/screens/DebugScreen.tsx`

特点：

- 负责 UI
- 负责用户操作
- 负责时序与表现
- 不应该成为核心规则的承载点

其中：

- `EventScreen` 更像“剧情播放器”
- `BattleScreen` 更像“战斗时序编排器”

---

## 4. `src/App.tsx`：总编排层

`App.tsx` 当前是项目的总控台。

它集中维护：

- `player`
- `screen`
- `storyEvent`
- `storyNodeId`
- `pendingBattleTransition`
- `challengeNpcId`
- `enemies`

关键职责：

- 切换 screen
- 进入地点后触发剧情
- 接住剧情系统返回的 command
- 接住战斗系统返回的结果
- 打开待处理的 world event

虽然很多逻辑已经下沉到 `src/game/appFlow.ts`，但 `App.tsx` 仍然是“系统连接器”。

---

## 三、项目最重要的两条主链

## 主链 1：剧情链

```text
地图选地点
→ query 找剧情事件
→ EventScreen 展示节点
→ engine 结算 choice / onEnter
→ appFlow 解释 transition
→ 回剧情 / 进战斗 / 回主界面
```

### 对应模块

#### 1. 地点进入后找事件
- `src/game/story/query.ts`

规则是：

- 按地点 `events` 数组顺序扫描
- 取第一个满足条件且未完成的事件
- 若都不匹配，回退到通用 `STORY_EVENTS`

所以地点 `events` 的顺序就是剧情优先级。

#### 2. 事件内部流转
- `src/game/story/engine.ts`

负责：

- `enterNode()`：进入节点并处理 `onEnter`
- `visibleChoices()`：过滤可见选项
- `resolveChoice()`：结算 consequences 与 transition

#### 3. 展示层承接
- `src/screens/EventScreen.tsx`

负责：

- 进入节点
- 展示文本
- 分页与对白拆解
- 书信样式展示
- 把结果交回 `onResolve`

#### 4. 系统级路由
- `src/game/appFlow.ts`
- `src/App.tsx`

`resolveStoryFlow()` 会把剧情 transition 归约成 App 可以执行的 command：

- `return-main`
- `goto-node`
- `start-battle`
- `goto-event`

---

## 主链 2：战斗链

```text
剧情或调试进入战斗
→ adapter 把 Player / Enemy 转为 Combatant
→ battle engine 纯函数结算
→ BattleScreen 负责时序和表现
→ appFlow 把结果接回剧情或主界面
```

### 对应模块

#### 1. 进入战斗
- `src/game/appFlow.ts`
- `src/App.tsx`

剧情里的 `battle` transition 会转成：

- 敌人列表
- 待处理 battle transition
- 进入 `BattleScreen`

#### 2. 战斗内部模型
- `src/game/battle/types.ts`

战斗引擎只认：

- `Combatant`
- `BattleSkill`
- `BattleState`

它不直接依赖外层 `Player` 存档结构。

#### 3. 行动推进与结算
- `src/game/battle/engine.ts`

核心能力：

- 多单位
- ATB / CTB 推进
- 多目标解析
- 状态系统
- AI 决策
- 纯函数输出新状态

#### 4. 战斗结束后回流
- `src/game/appFlow.ts`

`resolveBattleFlow()` 负责区分：

- 剧情战斗 → 返回结果页 / 后续剧情
- 普通战斗 / 切磋 → 直接回主界面

---

## 四、世界回响是如何嵌入主循环的

这是当前项目很有代表性的设计。

流程不是“直接弹出事件”，而是：

```text
回主界面
→ pollWorldEvent()
→ 命中一个 world event
→ 写入 triggeredEvents
→ 把 event id 放进 pendingWorldEvents
→ MainScreen 显示待处理消息
→ 用户手动打开
```

对应文件：

- `src/game/story/worldScheduler.ts`
- `src/game/appFlow.ts`
- `src/screens/MainScreen.tsx`
- `src/App.tsx`

这个设计的优点：

1. 不强行打断主流程
2. 更像“江湖来信 / 夜访 / 消息”
3. 便于后续扩展消息队列与主界面露出体验

---

## 五、当前已成型的几个关键子系统

## 1. 声明式剧情系统

不是把剧情硬编码在组件里，而是通过：

- `Condition`
- `Consequence`
- `Transition`
- `StoryNode`
- `StoryEvent`
- `WorldState`

形成了一套可扩写的剧情 DSL。

## 2. 多对多战斗系统

战斗不是旧式单挑原型，而是已经支持：

- 多对多
- ATB / CTB 行动值推进
- 多种目标模式
- 状态与护盾
- 追击势
- 内功催动外功

## 3. 队伍 / 羁绊 / 支援系统

队伍系统已经不只是概念：

- `world.party` 已持久化
- 出战 / 候补编队已存在
- 队友固定加成、羁绊收益、触发型支援已接入战斗

## 4. 存档与迁移系统

当前存档方案：

- `localStorage`
- 单 key `jinyong-save`
- Base64 导入 / 导出
- `loadPlayer()` 与 `importSaveCode()` 都会强制跑迁移

---

## 六、现在这套架构的优点

### 1. 分层清楚
数据、规则、展示、总编排分开，便于继续扩写。

### 2. 剧情与战斗都已经“引擎化”
新增内容时，不必总往 screen 里堆 if/else。

### 3. `appFlow.ts` 非常关键
它把：

- 剧情 transition
- 战斗结果
- 主界面回流

统一翻译成 App 能执行的 command，是现在项目可维护性的关键节点。

### 4. 队伍系统已经进入正式玩法闭环
不是“未来再做”的想法，而是已经进入主界面、角色页、战斗页与世界状态的正式系统。

---

## 七、当前最该记住的边界

### 1. 剧情内容写在 `data/`
不要把具体剧情业务判断塞进 screen。

### 2. 剧情规则写在 `game/story/*`
不要让 `App.tsx` 直接承担剧情细则。

### 3. `BattleScreen` 负责时序，不负责公式
公式与核心结算应继续留在 `battle/engine.ts`。

### 4. `map.ts` 的 `events` 顺序就是逻辑
它不是单纯配置顺序，而是剧情优先级。

---

## 八、当前架构里的主要风险点

### 1. `App.tsx` 仍然偏重
虽然已有 `appFlow.ts` 分担，但 `App.tsx` 仍承担了很多系统接线工作。

### 2. 地点剧情优先级强依赖 `events` 手工排序
内容越多，排序维护成本会越高。

### 3. 文档与实现容易继续漂移
这轮虽然已校正，但后续若继续快速演化，仍可能再次分叉。

---

## 九、结论

当前项目最准确的定位是：

> **内容仍在扩写中，但底层组织方式已经明显超出原型拼装阶段。**

它已经具备：

- 可持续扩写的剧情底层
- 可独立演进的战斗底层
- 已正式接入的队伍与支援系统
- 清楚但仍需继续守住的模块边界

后续继续开发时，真正需要小心的不是“没架构”，而是：

> **如何在不断加内容的过程中，不让现有边界被慢慢冲垮。**
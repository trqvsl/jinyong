# 金庸群侠传 · 项目进展

> 最后更新：2026-07-03  
> 项目定位：致敬《金庸群侠传 2/3》的单机网页武侠 RPG。  
> 当前阶段：**从“可玩原型”进入“剧情骨架 + 世界状态 + 持续扩写”阶段**。

---

## 一、项目现在到了哪一步

这已经不再是一个只有“练功 / 打怪 / 学技能”的小 demo，而是一个已经具备以下骨架的可扩展武侠游戏：

1. **基础循环已完整可玩**：创建角色 → 修炼 → 闯荡 → 触发剧情 / 战斗 → 获得成长 → 回主界面继续推进。
2. **战斗系统已重构成独立引擎**：支持多对多、群攻、状态效果、内功催动、战斗动画。
3. **剧情系统已完成声明式升级**：剧情数据写在 `data/`，逻辑结算走 `game/story/`，界面只负责展示。
4. **世界状态已经落地**：NPC 关系、阵营态度、主线推进、flag、触发过的世界事件都记录在 `player.world`。
5. **射雕卷已经形成第一条可连续游玩的剧情卷**：主线 8 节点 + 地点支线扩写，总量已达 **20 个可玩剧情事件**。
6. **世界回响机制已经接通**：玩家回到主界面时，会根据世界状态自动浮现符合条件的事件。

一句话总结：

> **项目已经完成“底层架构搭建 + 第一条内容样板线落地”，后面进入“按同一架构持续填内容、加系统厚度”的阶段。**

---

## 二、核心设计原则

### 1. 数据驱动
- 武功、敌人、门派、地图、剧情都尽量写成数据。
- 新增内容优先改 `src/data/*`，而不是把 if/else 塞进界面。

### 2. 三层分离
- `data/`：纯数据
- `game/`：纯逻辑引擎
- `screens/`：纯展示

### 3. 选择要真正改变世界
- 不再做“点完选项只给你一段文本”的假选择。
- 选择会写入 `WorldState`，后面由条件判断和 world events 真正读取。

### 4. 可长期扩写
- 先把“写剧情”的方法论定住，再继续扩金庸其他作品。
- 当前射雕卷就是后续《天龙》《倚天》《笑傲》的模板。

### 5. 旧存档兼容
- 每次结构升级都要补迁移，不能靠“删存档重开”解决。

---

## 三、当前架构（最新版）

```text
玩家操作
  ↓
screens/* 界面层（展示 / 交互）
  ↓
App.tsx 路由编排层
  ↓
game/* 核心逻辑层
  ├─ battle/        战斗引擎
  ├─ story/         剧情引擎 / 条件 / 后果 / world scheduler
  ├─ attributes.ts  根基 → 战斗属性推导
  └─ player.ts      存档 / 迁移 / 创建角色
  ↓
data/* 数据层
  ├─ skills / enemies / sects / items / shop
  ├─ map
  ├─ events
  └─ story/shendiao / worldEvents
  ↓
Player + WorldState
  ↓
localStorage 存档
```

### 分层约束

#### `src/data/`
- 放剧情、地图、NPC、敌人、武功等内容数据。
- **不负责流程控制**。

#### `src/game/`
- 放可复用引擎逻辑。
- **不认识具体剧情卷的文案语义**。

#### `src/screens/`
- 负责把当前状态渲染出来。
- **不直接承担规则判定**。

#### `src/App.tsx`
- 做屏幕切换、剧情/战斗衔接、返回主界面后的统一调度。

---

## 四、关键数据流

### 1. 地点剧情触发流

```text
主界面 → 地图 → 选择地点
  ↓
getStoryEventByLocation()
  ↓
按 events 数组顺序找第一个满足条件且未完成的事件
  ↓
EventScreen 展示节点
  ↓
玩家选择 → resolveChoice()
  ↓
applyConsequences() 写入 player/world
  ↓
transition 决定 end / goto / branch / battle / gotoEvent
```

### 2. 战斗衔接流

```text
剧情选项触发 battle
  ↓
BattleScreen
  ↓
resolveBattleOutcome()
  ↓
返回剧情结果页 / 后续节点
  ↓
结束后统一回主界面
```

### 3. 世界回响流

```text
任何子界面返回主界面
  ↓
App.returnToMain()
  ↓
pollWorldEvent(player, world)
  ↓
扫描 WORLD_EVENTS
  ↓
若命中：直接跳 EventScreen
否则：正常显示 MainScreen
```

这意味着“世界事件”不再依赖某个地点，而是依赖**你之前做过什么**。

---

## 五、已经完成的系统能力

## 5.1 基础玩法循环
- ✅ 角色创建
- ✅ 随机资质 / 八大根基
- ✅ 天数推进
- ✅ 闭关修炼
- ✅ 外出闯荡
- ✅ 战斗胜败结算
- ✅ 经验 / 升级
- ✅ 本地存档 + 继续游戏

## 5.2 战斗系统
- ✅ 多对多战斗引擎
- ✅ 回合调度与行动顺序
- ✅ 单体 / 横扫 / 双击 / 乱打
- ✅ 中毒 / 回春 / 眩晕 / 护盾 / 增减益
- ✅ 内功催动外功
- ✅ 战斗内使用道具
- ✅ 战斗表现：飘字、抖动、技能飞字、暴击震屏

## 5.3 角色成长系统
- ✅ 八大根基属性
- ✅ 根基 → 战斗属性推导
- ✅ 资质影响修炼收益
- ✅ 升级属性点分配
- ✅ 属性页展示与重算

## 5.4 门派 / 武功 / 道具 / 商店
- ✅ 8 个门派
- ✅ 16+ 武功
- ✅ 商店入口与基础货品
- ✅ 背包 / 行囊系统
- ✅ 角色页与战斗内使用道具

## 5.5 剧情系统（当前重点）
- ✅ 声明式 `Condition / Consequence / Transition`
- ✅ `WorldState` 挂在 `player.world`
- ✅ 节点式剧情流转（`goto / branch / random / battle / autoNext`）
- ✅ once 事件只触发一次
- ✅ 旧存档迁移兼容

---

## 六、当前内容量级

## 6.1 射雕卷

### 主线 8 个关键事件
1. 牛家村·风雪惊变
2. 大漠·少年射雕
3. 中原·客栈奇缘
4. 洪七公·叫花传功
5. 赵王府·夜探藏经
6. 桃花岛·东邪试炼
7. 铁枪庙·恩仇了断
8. 华山论剑·天下五绝

### 新增地点支线 12 个
- 牛家村：雪夜余波、穆念慈相关事件
- 临安：丐帮宴、岳武穆遗壁、夜谈等
- 大漠：白雕、草原宴、江南七怪余波等
- 桃花岛：箫声、迷阵等
- 华山：雪夜、石刻等

### 当前总量
- ✅ **20 个可玩射雕事件**（主线 + 地点支线）

这一步很关键：

> 说明项目已经从“只有几段样板剧情”进入“一个作品卷开始有厚度”的阶段。

## 6.2 世界回响事件

当前已接入 5 个 world events：
- 洪七公来访
- 全真来信
- 黄蓉夜访
- 郭靖寄药
- 白驼密邀

它们分别覆盖：
- 正线倾向
- 邪线倾向
- 阵营态度
- NPC 好感
- 主线推进后的回响

---

## 七、本轮（2026-07-02）完成的重点工作

这一轮不是简单“补几段文案”，而是把剧情体验从“可跑”提升到“可读、可感、可扩”。

## 7.1 事件文本体验重构

### 已完成
- ✅ 长文本改为**分页点击推进**，不再整段砸脸显示
- ✅ 事件页支持**混合旁白 + 对话框**显示
- ✅ 自动识别中文引号中的对白，拆成独立对白块
- ✅ 自动根据上下文推断说话人
- ✅ 结果文本也走同一套对白拆解逻辑

### 解决的问题
- 过去事件文本太像“说明书”，缺乏临场感
- 旁白、对白、结果全挤在同一种块里，层次不清
- 有对白时没有人物说话的视觉结构

## 7.2 EventScreen 重构

关键升级集中在 `src/screens/EventScreen.tsx`：

- ✅ 新增脚本分段结构 `ScriptSegment`
- ✅ 新增 `inferSpeaker()` 自动推断说话人
- ✅ 新增 `parseStorySegments()` 自动拆分“叙事 + 引号对白 + 叙事”
- ✅ 新增 `buildScriptPages()` 按页组织脚本
- ✅ 增加“继续阅读”节奏控制
- ✅ 当可见选项被条件全部过滤时，不再卡死

## 7.3 射雕卷扩容
- ✅ 从“8 个主线节点”扩成“8 主线 + 12 支线”的第一卷骨架
- ✅ 所有新增事件都已经挂到地图地点上
- ✅ 地图进入后能按地点顺序自然露出内容

## 7.4 世界事件做厚
- ✅ 新增 `src/data/story/worldEvents.ts`
- ✅ 新增 `src/game/story/worldScheduler.ts`
- ✅ 回主界面统一扫描 world events
- ✅ 世界事件不再只靠地点触发，而是靠世界状态自然浮现

## 7.5 一批关键体验 bug 修复

### 已修复
- ✅ 去掉“旁白”字样标签，叙事更自然
- ✅ 修正牛家村首战战前结果文本的剧透
- ✅ 修复结果文本里的对白不进对话框的问题
- ✅ 修复混合文本被整段错误标注 speaker 的问题
- ✅ 修复部分回主界面路径不扫描 world events 的问题
- ✅ 修复 `aptitude` 与 `roots.comprehension` 不同步
- ✅ 修复主界面修炼直接写 attack/defense，绕开根基推导的问题

## 7.6 队友 / 羁绊系统阶段 1 里程碑（2026-07-03）

这一轮完成的不是单个按钮，而是一整套“队友真正进入战斗系统”的闭环：

- ✅ `WorldState.party` 落地，队伍状态进入世界存档
- ✅ 出战 / 候补编队与归一化逻辑落地
- ✅ 主界面、角色页、战斗页全部接通队伍展示
- ✅ 队友固定随行加成与羁绊收益落地
- ✅ 开场支援、触发型支援、战斗高亮联动落地
- ✅ 轻量机制收益接入战斗结算（护盾 / 攻防速增益 / 追击势 / 回血 / 回内）
- ✅ 支援规则沉淀为可描述结构，并在主界面 / 角色页 / 战斗页同步展示
- ✅ 郭靖、黄蓉、靖蓉同心形成首批角色 / 羁绊特化原型

这一里程碑的意义在于：

> **队友系统已经从“剧情附属物”升级为真正的战斗系统子模块，并具备继续做精细化设计的稳定骨架。**

---

## 八、当前关键文件与职责

### 剧情 / 世界状态
- `src/data/story/shendiao.ts`：射雕卷主线 + 支线数据
- `src/data/story/worldEvents.ts`：世界回响事件
- `src/game/story/worldScheduler.ts`：世界事件调度器
- `src/game/story/consequences.ts`：后果解释器
- `src/game/story/conditions.ts`：条件解释器
- `src/game/story/engine.ts`：剧情流转引擎

### UI / 路由
- `src/screens/EventScreen.tsx`：剧情展示、对白拆解、分页推进
- `src/App.tsx`：剧情/战斗/主界面返回编排
- `src/App.css`：事件页对白框与叙事样式

### 数值 / 存档
- `src/game/player.ts`：角色创建、迁移、存档
- `src/game/attributes.ts`：根基推导战斗属性
- `src/screens/MainScreen.tsx`：修炼入口

### 地图 / 内容挂载
- `src/data/map.ts`：地点、事件列表、解锁条件

---

## 九、当前验证状态

本轮修改后，核心验证目标已经达成：

- ✅ `npm run build` 通过
- ✅ TypeScript 0 错误
- ✅ `verify-story` 通过
- ✅ `verify-battle` 通过
- ✅ `verify-fixes` 通过

这说明当前并不是“边改边堆、能跑就行”的脆弱状态，而是已经有一定工程守护。

---

## 十、项目的现状判断

### 现在最强的地方
1. **底层架构已经稳定**：三层清晰，后续继续加内容不会越来越乱。
2. **剧情系统方向是对的**：世界状态 + 声明式条件/后果，比传统 flag 堆叠更耐扩展。
3. **射雕卷已经证明这套体系能支撑“连续剧情 + 支线 + 世界回响”。**
4. **体验层开始成型**：对白框、分页推进，让事件从“看文档”变成“在玩剧情”。

### 现在还不够的地方
1. **world events 数量还不够多**，虽然机制通了，但厚度还只是开头。
2. **NPC 互动仍偏“事件型”**，还没完全形成持续陪伴感。
3. **队友、装备、结局大盘还未真正落地。**
4. **更多作品卷还没开始铺。**

---

## 十一、接下来的方向（按优先级）

## P1：继续做厚剧情系统

### 目标
- 把“射雕卷”从能玩，做到真正有连续追玩欲。

### 具体方向
1. **继续补射雕支线密度**
   - 临安、牛家村、桃花岛、华山继续补可重复 / 条件触发支线
2. **扩 world events 池**
   - 让 NPC 主动找你、江湖风声主动传来、阵营报复/馈赠主动发生
3. **把 NPC 关系写活**
   - 不只是“加 relation 数值”，而要有更多“关系回响”

## P2：把世界状态真正转成可见变化

### 目标
- 让玩家感到“我的选择改变了江湖”，而不仅是数值变了。

### 具体方向
1. 地点事件根据前情变化
2. 阵营态度影响商店 / 战斗 / 师门 / 传讯
3. NPC 生死、立场、亲疏影响后续剧情露出

## P3：把系统厚度补齐

1. **队友入队系统**：让 NPC 不只是剧情对象，而是实战成员
2. **装备系统**：武器 / 防具 / 饰品形成长期追求
3. **Boss / 特殊遭遇**：让战斗与剧情更紧密
4. **结局系统大盘**：不只是一卷结局，而是整个江湖走向

## P4：扩到更多作品卷

当射雕卷的方法完全跑顺后，可以按同架构继续铺：
- 天龙八部
- 倚天屠龙记
- 笑傲江湖
- 神雕侠侣

原则不是一口气全铺，而是：

> **先把“一个作品卷如何做厚”跑通，再复制到其他作品。**

---

## 十二、当前结论

这个项目当前最重要的成果，不是“又多了几个按钮”，而是三件事：

1. **世界状态架构已经立住**
2. **射雕卷已经成为可扩写的第一条剧情样板线**
3. **事件体验已经从纯文字堆砌，升级到带节奏和对白层次的可玩形态**

因此，后续开发的主旋律已经很明确：

> **不再优先堆零散功能，而是沿着“剧情厚度 + 世界回响 + NPC关系 + 长线养成”这条路，把整个江湖做活。**

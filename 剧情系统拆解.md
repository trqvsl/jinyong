# 金庸群侠传 · 剧情系统拆解（方案 B）

> 目的：把当前剧情系统从“文件列表”提升为“机制说明”，供 review 使用。  
> 依据：以 `src/data/story/*`、`src/game/story/*`、`src/game/appFlow.ts`、`EventScreen / AreaScreen / SceneTransition` 的真实实现为准。
> 本文是机制导览；字段与边界的 canonical 说明以《剧情系统设计手册.md》为准。

---

## 一、先说结论

当前剧情系统的本质不是“很多地点弹出很多事件”，而是：

> **一套由共享世界状态驱动的声明式剧情系统。**

它成立的关键不是单个事件文件，而是六个层面一起工作：

1. 统一的数据模型（DSL）
2. 统一的世界状态（WorldState）
3. 统一的查询与写入机制（Condition / Consequence）
4. 统一的流转解释器（engine / query / worldScheduler / appFlow）
5. 统一的展示层（EventScreen / StoryStage）
6. 第一幕试点的地点层（AreaScreen / SceneTransition）

---

## 二、第一层：剧情 DSL 长什么样

核心定义都在：

- `src/data/story/schema.ts`

最重要的五类对象如下。

## 1. `Consequence`

表示“这次选择 / 这个结果，会改什么”。

当前已经支持修改：

- 玩家数值：声望、karma、gold、hp、mp、exp 等
- 道具与技能
- NPC 生死 / 阵营 / fateTags / relationType
- 阵营态度与势力
- arcBeat / arcVariant / arcEnding
- flag

所以它的本质是：

> **声明式写 player / world。**

---

## 2. `Condition`

表示“什么情况下可见 / 可触发 / 可分支”。

当前支持判断：

- `karma`
- `reputation`
- `relation`
- `npcAlive`
- `npcRecruited`
- `npcHasTag`
- `npcRelationType`
- `factionAttitude`
- `arcBeat`
- `arcVariant`
- `flag`
- `hasItem`
- `hasSkill`
- `and / or / not`

所以它的本质是：

> **声明式查 player / world。**

---

## 3. `Transition`

表示“接下来去哪里”。

当前支持：

- `end`
- `pause`
- `goto`
- `branch`
- `random`
- `battle`
- `gotoEvent`
- `gameOver`

这意味着剧情系统不只会“跳下一段”，还支持：

- 自动条件分流
- 加权随机分流
- 跨事件跳转
- 剧情战斗
- 坏结局

---

## 4. `StoryNode`

表示事件中的一个节点。

它可以是：

- 选择节点
- 纯叙事节点
- 自动条件分叉节点
- 终点节点
- 带 `stage` 的多角色舞台节点
- 带 `sceneTransition` 的地点 / 时辰转场节点

也就是说，节点不是固定的“标题 + 文案 + 选项”模板，而是一个更通用的叙事单元。

---

## 5. `StoryEvent`

表示一个完整事件。

它通常包含：

- `id`
- `entryNode`
- `nodes`
- `condition?`
- `once?`

本质上就是：

> **一个小型状态图。**

---

## 三、第二层：WorldState 如何承载“江湖记忆”

定义位置：

- `src/data/story/schema.ts`

当前真实字段包括：

- `version`
- `npcs`
- `factions`
- `arcs`
- `flags`
- `party`
- `pendingWorldEvents`
- `triggeredEvents`
- `seenNodes`
- `completedEvents`
- `currentStory`

---

## 1. `npcs`

这是 NPC 的命运态，而不是普通图鉴。

字段包括：

- `alive`
- `recruited`
- `faction`
- `fateTags`
- `relationType`

需要特别注意的是：

- 数值好感在 `player.relations`
- 语义关系在 `world.npcs[npcId].relationType`

所以当前 NPC 关系实际上是两层：

### 数值层
- 关系值高低

### 语义层
- 初识 / 朋友 / 知己 / 师徒 / 挚友 / 恋人 / 仇敌 等

这比单纯好感条更适合武侠叙事。

---

## 2. `factions`

这是阵营层：

- `attitude`
- `power`

当前主要用于剧情态度与后续扩展，未来可以继续用于门派、来信、报复、招揽等分支。

---

## 3. `arcs`

这是主线 / 事件链进度层。

当前射雕卷就是靠它串联的，典型 beat 包括：

- `niujia`
- `damos`
- `meet-rong`
- `qigong`
- `wangfu`
- `taohua`
- `yangkang`
- `huashan`

---

## 4. `flags`

这是兜底字段。

优点：
- 灵活

风险：
- 用多了容易回到“flag 地狱”

所以更推荐优先使用：

- `arcBeat`
- `npcRelationType`
- `npcTag`
- `npcAlive`
- `factionAttitude`

---

## 5. `party`

这是队伍状态。

结构：

- `activeNpcIds`
- `reserveNpcIds`

它说明：

> 队伍系统已经不是 UI 临时态，而是正式进入 `WorldState` 的长期状态。

---

## 6. `pendingWorldEvents`

表示：

> 已被世界调度命中的回响事件，但玩家还没有打开。

这使“江湖消息 / 夜访 / 来信”形成了一个真正的待处理队列。

---

## 7. `triggeredEvents`

这是 world event 的 once 去重集。

## 8. `completedEvents`

这是普通 `once: true` 事件的完成记录。

---

## 9. `seenNodes`

这是 `onEnter` 的去重记录。

当前实现已经改为：

> 按 **事件作用域节点键** 去重，即 ``${event.id}:${nodeId}``。

这意味着即使多个事件都用了：

- `main`
- `result`
- `arrival`

它们的 `onEnter` 也会分别按所属事件独立结算，不再互相污染。

---

## 10. `currentStory`

保存当前事件、节点、phase、页码、地点、待处理 transition、剧情战敌方以及 `paused / areaEntry`。

它能恢复剧情节点、结果页和剧情战开场；但不保存战斗回合内状态，也不保存独立空间中的当前子地点。

---

## 四、第三层：Condition / Consequence 是怎么工作的

## 1. Consequence：怎么写状态

实现位置：

- `src/game/story/consequences.ts`

它做的事情大致是：

1. 拷贝 player
2. 拷贝 world
3. 根据 `kind` 分发处理
4. 对有界字段做 clamp
5. 若 karma 改了，则重算 alignment
6. 最后把 `world` 回写到 `player.world`

其中最关键的约束是：

```ts
p.world = w
```

也就是：

> **任何剧情后果如果改了 world，都必须同步回写 `player.world`。**

否则 arcBeat、flags、NPC 命运等都会丢失。

---

## 2. Condition：怎么查状态

实现位置：

- `src/game/story/conditions.ts`

它的价值在于：

- 统一剧情作者的表达方式
- 自动处理 missing key 默认值
- 让事件、节点、选项、branch、world event 共用同一套判断逻辑

所以剧情作者写的是：

- “是否满足这个条件”

而不是：

- “去读一大堆 world 嵌套字段再自己 if/else”

---

## 五、第四层：剧情到底是怎么跑起来的

要看四个文件一起工作：

- `engine.ts`
- `query.ts`
- `worldScheduler.ts`
- `appFlow.ts`

---

## A. `query.ts`：决定“这次遇到哪个事件”

位置：

- `src/game/story/query.ts`

当前逻辑：

```text
进入世界地图地点
→ 读取该地点的 events 数组
→ 从前往后找第一个“未完成 + 条件满足”的事件
→ 若找到且有匹配的内部地图入口，建立 paused + areaEntry checkpoint
→ AreaScreen 地图 → 独立空间 → 剧情续接动作
→ 若没有内部地图，直接返回事件
→ 若都找不到，回退通用 STORY_EVENTS
```

所以地点剧情露出不是随机的，而是：

> **地点配置顺序 + 条件系统一起决定。**

这也是为什么主线事件必须排在通用事件前。

---

## B. `engine.ts`：决定“事件内部怎么走”

位置：

- `src/game/story/engine.ts`

它主要负责三步：

### 1. `enterNode()`
进入节点并执行 `onEnter`（只在第一次进入时执行）。

### 2. `visibleChoices()`
根据 condition 过滤掉当前不可见的选项。

### 3. `resolveChoice()`
结算当前 choice 的 consequences，并返回 transition。

这个文件最重要的特征是：

> 它只负责剧情局部语义，不负责 screen 切换，也不负责主界面返回。

---

## C. `worldScheduler.ts`：决定“世界有没有新消息”

位置：

- `src/game/story/worldScheduler.ts`

它每次只做一件事：

- 从 `WORLD_EVENTS` 中找第一个满足条件的事件
- 如果它是 `once`，先写入 `triggeredEvents`
- 把 event 返回给上层

注意：

> 它不直接打开 UI，也不直接切 screen。

---

## D. `appFlow.ts`：决定“系统级如何跳”

位置：

- `src/game/appFlow.ts`

这是剧情系统当前非常关键的一层。

### 1. `resolveStoryFlow()`
输入：

- 当前 player
- 当前 transition
- 是否消耗天数
- 当前 storyEvent
- 当前 locationId

输出：

- 新 player
- App 可以执行的 command

command 可能是：

- `show-main`
- `show-area`
- `goto-node`
- `show-event-entry`
- `show-event-result`
- `show-battle`

### 2. `resolveBattleFlow()`
把战斗结果：

- `won`
- `partial`
- `lost`
- `fled`

重新翻译成：

- 直接回主界面
- 或一段结果文本 + 后续 transition

所以：

> `appFlow.ts` 的本质是把“引擎语义”翻译成“App 的可执行命令”。

这是当前剧情系统跑得比较稳的关键原因之一。

---

## 六、第五层：EventScreen 如何接住这套系统

位置：

- `src/screens/EventScreen.tsx`

它本身比表面看起来更薄。

### 它负责的事

#### 1. 进入节点
- 调 `enterNode()`

#### 2. 取可见选项
- 调 `visibleChoices()`

#### 3. 点选后结算
- 调 `resolveChoice()`

#### 4. 管理展示 phase
当前有三种：

- `choosing`
- `autoNext`
- `result`

#### 5. 管理阅读体验
通过 presenter 工具实现：

- 分页
- 对白拆分
- 书信样式
- 无文字继续角标
- `StoryStage` 多角色场景
- `sceneTransition` 地点 / 时辰题签

所以 EventScreen 的真实角色不是“剧情引擎”，而是：

> **剧情播放器 + 阅读器。**

`sceneTransition` 是纯展示字段，不属于剧情 `Transition`，也不会自动修改 `day` 或 checkpoint。

---

## 七、第六层：地点内部空间如何接入

当前只在牛家村启用：

```text
paused / areaEntry checkpoint
→ AreaScreen 地点地图
→ 点击点位直接进入 StoryAreaSpace
→ 交谈 / 调查 / 商店 / 行囊 / 切磋 / 离开
→ 命中 storyTargets 的空间可恢复 EventScreen
```

关键文件：

- `src/data/story/areaMaps.ts`
- `src/screens/AreaScreen.tsx`
- `src/screens/SceneTransition.tsx`
- `src/App.tsx`

当前边界：

- 只有牛家村配置了内部地图
- 驻场人物与动作是静态数据，不支持 Condition
- 商店、行囊和切磋复用现有通用系统
- 当前子地点与工具页回流是 App 内存态，不写入 WorldState

---

## 八、世界回响当前的真实用户流

这部分特别值得单独写清楚，因为它和“直接弹窗事件”的直觉不一样。

当前流程是：

```text
玩家回主界面
→ normalizeMainPlayer()
→ pollWorldEvent()
→ 命中一个 world event
→ 写入 triggeredEvents
→ 把 event id 放进 pendingWorldEvents
→ MainScreen 显示待处理消息
→ 玩家点击打开 EventScreen
```

这带来了三个效果：

1. 世界回响更像“江湖消息”
2. 不强行打断玩家当前体验
3. 允许后续继续扩主界面的消息系统

---

## 九、剧情与战斗是如何衔接的

## 1. 剧情进入战斗

`resolveStoryFlow()` 会把 `battle` transition 转成：

- 敌人列表
- 待处理 battle transition
- 进入 `BattleScreen`

## 2. 战斗返回剧情

`resolveBattleFlow()` 再根据：

- `won`
- `partial`
- `lost`
- `fled`

调用 `resolveBattleOutcome()`，得到：

- 结果文本
- consequences
- 后续 `then` transition

然后再回到 `EventScreen` 的 `result` 阶段。

## 3. 致命战斗

当 `battle.lethal === true` 且战败时，会走 `gameOver` 结果。

---

## 十、这套剧情系统当前最强的地方

### 1. 内容与规则分开了
剧情数据写在 `data/`，规则写在 `game/story/*`。

### 2. 共享世界状态是真的成立了
不是每个事件各自为战，而是所有事件都在读写同一份 world。

### 3. 世界回响已经形成闭环
`WORLD_EVENTS + pollWorldEvent + pendingWorldEvents` 已经不是概念，而是正式运行中的机制。

### 4. 展示层已经不只是文本框
EventScreen 已具备分页、头像对白、多角色舞台与转场题签；牛家村进一步验证了地点地图和独立空间。

---

## 十一、当前剧情系统的主要局限

### 1. 断点只持久化到剧情节点 / 战斗开场
`currentStory` 已经保存事件、节点、phase、页码和剧情战入口。

这意味着：

> 刷新可恢复剧情节点，但战斗从开场重启；独立空间中的当前子地点、商店或行囊页不会持久化。

### 2. 地点露出顺序强依赖 `events` 排列
前期方便，后期内容一多，维护成本会增大。

### 3. 地点空间仍是第一幕试点
只有牛家村接入，驻场人物 / 动作不能按 Condition 动态变化。

### 4. world event 每轮只入队一个
稳定，但也意味着多个符合条件的回响会被顺延。

---

## 十二、总结

如果把当前剧情系统压缩成一句话：

> **它已经是一套“世界状态驱动的声明式叙事系统”，而不只是若干地点弹窗。**

它的核心链路是：

- `schema.ts` 定义语言
- `state.ts` 提供世界记忆
- `conditions / consequences` 负责查与写
- `engine / query / worldScheduler / appFlow` 负责运行
- `EventScreen / StoryStage` 负责事件演出
- `AreaScreen / SceneTransition` 在第一幕承载地点停留与空间切换

八幕因果链路已经立住；地点生活感与章节演出模板仍需在第一幕收口后再推广。

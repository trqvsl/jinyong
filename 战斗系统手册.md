# 金庸群侠传 · 战斗系统手册

> 这份文档是**当前战斗模块的设计说明 + 代码索引**。  
> 它描述的是现在代码已经落地的结构、机制与注意事项，不再把已实现内容写成“规划中”。

---

## 〇、一句话概括

当前战斗系统已经形成：

> **纯函数战斗引擎 + 适配层 + BattleScreen 时序编排** 的完整闭环。

它不是旧式的单挑原型，而是已经支持：

- 多对多战斗
- CTB / ATB 式行动顺序推进
- 多种目标模式
- 状态系统
- 队友 / 羁绊支援接入
- 剧情战斗与普通战斗打通

---

## 一、代码地图（真实位置）

当前战斗相关代码主要分布在以下位置：

| 文件 | 作用 |
|---|---|
| `src/game/attributes.ts` | 根基属性 → 玩家面板战斗属性推导 |
| `src/game/battle/types.ts` | 战斗引擎内部类型（Combatant / BattleSkill / BattleState 等） |
| `src/game/battle/engine.ts` | 战斗核心纯函数引擎 |
| `src/game/battle/adapter.ts` | `Player / Enemy / NPC` 与引擎内部类型的适配层 |
| `src/game/battle/flow.ts` | ATB 调度、支援落地与战后收尾编排 |
| `src/game/progression.ts` | 统一经验升级、武功熟练度增长与战斗修正 |
| `src/game/party.ts` | 队友 / 羁绊支援与战斗加成规则 |
| `src/screens/BattleScreen.tsx` | 战斗 UI、行动时序、动画与战斗日志展示 |
| `src/game/appFlow.ts` | 剧情战斗与普通战斗回流编排 |

> 旧文档中把战斗逻辑写成 `src/game/battle.ts` 已过时；当前已拆成 `battle/` 子目录结构。

---

## 二、整体分层

```text
Player / Enemy / NPC（游戏外层数据）
  ↓ adapter
Combatant / BattleSkill / BattleState（战斗内部模型）
  ↓ engine
纯函数结算（ATB / 伤害 / 状态 / 目标解析 / AI）
  ↓
BattleScreen.tsx
  负责 UI、播放节奏、日志、动画、交互
  ↓
App.tsx / appFlow.ts
  负责把战斗结果接回剧情或主界面
```

### 分层原则

- `battle/types.ts` / `battle/engine.ts` 不依赖 React
- 引擎不直接认识 `Player` 存档结构
- 根基属性、NPC 队伍、剧情战斗衔接都由外层适配 / 编排层完成

这让战斗系统成为真正可独立迭代的模块。

---

## 三、战斗内部模型

定义在 `src/game/battle/types.ts`。

### 1. Combatant
引擎只认识 `Combatant`，不直接认识 `Player` / `Enemy`：

- `uid`
- `side`
- `name`
- `hp / hpMax`
- `mp / mpMax`
- `attack / defense / speed`
- `statuses`
- `skills`
- `atb`
- `critRate?`
- `dodgeRate?`
- `innerPower?`

其中：

- `critRate` / `dodgeRate` / `innerPower` 通常由适配层从玩家根基属性推导后透传
- 这保证 battle engine 不必知道角色根基来源

### 2. BattleSkill
引擎内部武功结构：

- `id`
- `name`
- `category`
- `damageType?`
- `power`
- `mpCost`
- `effect?`
- `targeting?`
- `innerScale?`

### 3. BattleState
一场战斗的完整状态：

- `playerSide: Combatant[]`
- `enemySide: Combatant[]`
- `atbThreshold`
- `objective?`

这说明当前战斗早已不是单体对单体原型，而是多对多模型。

---

## 四、行动顺序：ATB / CTB 已落地

这一部分现在**已经实现**，不再是“规划中”。

核心函数在 `src/game/battle/engine.ts`：

- `advanceAtb()`
- `nextActor()`
- `applyAtbConsume()`
- `previewTurnOrder()`

### 当前机制

```text
所有存活单位都有 atb
  ↓
按 speed 推进行动值
  ↓
atb 达到阈值者获得行动权
  ↓
行动后扣除一个阈值，保留余量
```

剧情战的“完整轮”按当前所有存活单位都至少获得过一次行动权计算：

- 高速单位在慢速单位出手前重复行动，不会重复计轮
- 眩晕跳过仍视为已经获得过本轮行动权
- 已倒下单位不再阻塞当前轮完成

### 这带来的效果

- 速度不再只是修饰属性，而是直接影响出手频率
- 多单位战斗天然成立
- BattleScreen 可以显示未来行动顺序预览

所以：

> **当前战斗系统已经是多单位、行动值驱动的 CTB/ATB 式战斗，而不是严格你一下我一下。**

---

## 五、目标模式：当前真实支持范围

定义在 `battle/types.ts`，目标解析在 `battle/engine.ts -> resolveTargets()`。

当前支持：

- `single`：单体
- `all-enemy`：敌方全体
- `spread2`：至多两个目标
- `random3`：随机三个目标
- `self-side`：我方全体

这意味着战斗系统已经原生支持：

- 横扫
- 双击
- 乱打
- 群体增益 / 我方全体内功

---

## 六、根基属性如何进入战斗

根基属性定义在 `src/types/index.ts`，推导公式在 `src/game/attributes.ts`。

### 八大根基
- 力量
- 外功
- 内功
- 悟性
- 身体
- 吐纳
- 身法
- 福缘

### 推导关系
战斗系统并不直接存“根基状态机”，而是由外层先把玩家根基推导为：

- `attack`
- `defense`
- `hpMax`
- `mpMax`
- `speed`
- `critRate`
- `dodgeRate`
- `innerPower`

再适配为 `Combatant` 交给引擎。

这保证了：

- 角色成长系统独立演进
- 战斗引擎不依赖存档细节

---

## 七、伤害与出招分流

核心函数位于 `src/game/battle/engine.ts`：

- `resolveExternalAttack()`
- `performSkill()`
- `performPlayerSkill()`
- `performEnemySkill()`
- `performAction()`

### 1. 外功攻击
当前外功结算包含：

- 攻防差
- 随机浮动
- 闪避
- 暴击
- 护盾抵消
- 追击势追加伤害
- 部分附带状态效果

### 2. 内功催动
`resolveExternalAttack()` 会按：

- `attacker.innerPower`
- `skill.innerScale`

临时给攻击加成。

这就是“内功催动外功”的真正落地点。

### 3. 武功分流
按 `skill.category` 分成：

- **外功**：直接伤害
- **奇门**：伤害 + 控制 / 异常
- **内功**：自身 / 我方增益、护盾、回春等
- **轻功**：身法类增益

---

## 八、状态系统（当前已落地）

定义在 `battle/types.ts`，主要结算在 `battle/engine.ts`：

- `tickStatuses()`
- `isStunned()`
- `applyStatusToCombatant()`
- `healCombatant()`
- `restoreMpCombatant()`

### 当前状态种类
- `poison` 中毒
- `buff-atk` 攻击强化
- `buff-def` 防御强化
- `buff-spd` 身法提升
- `buff-chase` 追击势
- `heal` 回春
- `stun` 眩晕
- `shield` 护盾

### 即时效果
除了挂在状态栏里的状态，还支持即时数值型效果：

- `healCombatant()`：即时回血
- `restoreMpCombatant()`：即时回内力

这部分已经被队友支援系统真实使用，不再只是预留接口。

---

## 九、回合开始时序

### 当前关键时序特征
当前多单位战斗中，单位在获得行动前，会对自己执行状态结算：

- `tickUnitStatuses()`
- 检查是否被眩晕
- 眩晕则跳过出招

这与旧的一对一“敌方回合开始统一结算”认知不同。  
现在的语义更接近：

> **谁准备行动，就先处理谁身上的持续状态。**

---

## 十、敌人 AI

当前 AI 实现在 `battle/engine.ts`：

- `enemyChooseSkill()`
- `enemyDecideAction()`

### 当前策略
- 若残血且会内功且内力足够 → 倾向用内功保命
- 否则从可用的外功 / 奇门中挑高 power 招式
- 单体招式默认优先找对方血量最低者收割

这还不是很复杂的 Boss AI，但已经不是完全随机乱出招。

---

## 十一、队友 / 羁绊支援如何接入战斗

这是当前战斗系统的一大特色，规则主要在 `src/game/party.ts`。

### 1. 固定随行收益
出战队友会给主角提供常驻：

- attack
- defense
- speed

### 2. 羁绊收益
例如：

- 靖蓉同心
- 并肩默契

会进一步提供队伍级别加成。

### 3. 开场支援文案
BattleScreen 可展示队友与羁绊的开场提示。

### 4. 触发型支援
当前已实现三类触发：

- `crit`：主角暴击时
- `guard`：主角进入危局时
- `victory`：战斗获胜时

### 5. 触发后的真实战斗效果
支援不是只写日志，而是会施加：

- 护盾
- 攻 / 防 / 速 buff
- 追击势
- 回血
- 回内力

也就是说：

> **队友系统已经不是剧情摆设，而是战斗系统的正式子模块。**

---

## 十二、经验与武功熟练度

成长规则集中在 `src/game/progression.ts`，battle engine 不直接认识 `Player.mastery`。

### 1. 统一经验升级

- 剧情 `exp` consequence 与战斗奖励都调用 `applyExperience()`
- 单次奖励可连续升级多级
- 每级获得 5 点属性点
- 升级后重新推导面板并回满气血、内力

### 2. 熟练度增长

- 只有主角在战斗中实际施展的武功会计次
- 胜利、战败、逃跑都会保留本场练习所得
- 单场每门武功最多计 5 次，熟练度上限为 100
- 悟性低于 60 时每次 +1，达到 60 时每次 +2，达到 85 时每次 +3

### 3. 熟练度的战斗作用

`battle/adapter.ts` 在把 `Skill` 转成 `BattleSkill` 时应用熟练度：

- 100 熟练度最多增加 10% 招式威力
- 每 25 点熟练度提升 1 点非零状态强度
- 100 熟练度最多增加 10 个百分点的状态命中率

角色页按初习、入门、熟练、精通、圆满五档展示熟练度和当前实际增益。旧存档加载时会为已学武功补 0，并把异常值夹断到 `0–100`。

---

## 十三、剧情战斗与普通战斗如何衔接

桥接主要在 `src/game/appFlow.ts` 与 `src/App.tsx`。

### 1. 剧情战斗
当剧情 transition 为 `battle` 时：

- `resolveStoryFlow()` 构造敌人
- `allyIds` 注入本场剧情友方，并与正式出战队友按 NPC id 去重
- `objective` 转成 battle runtime 目标
- `App.tsx` 进入 `BattleScreen`
- 战斗结束后 `resolveBattleFlow()` 按 `onWin / onLose / onFlee` 返回剧情结果页

当前支持两类基础目标：

- `defeatAll`：击退全部敌人
- `surviveRounds`：守满指定完整轮数

两类目标都可附加 `protectAllyId`：

- 指定友方倒下立即判败
- 守回合时提前清场仍判胜
- 生存胜利只结算实际击倒敌人的经验和银两
- 战斗刷新后，目标、剧情友方和保护对象会随 transition 一并恢复

### 2. 非剧情战斗
例如：

- 调试战斗
- NPC 切磋

这种情况战斗结束后通常直接回主界面，并在必要时补 NPC 关系后果。

### 3. 致命战斗
若剧情战斗标了 `lethal: true` 且战败，会转成 `gameOver` 结果。

---

## 十四、BattleScreen 的职责

`src/screens/BattleScreen.tsx` 主要负责：

- 战斗画面展示
- 行动顺序 / turn order 可视化
- 玩家选招与选目标
- 调度纯函数引擎
- 动画 / 飘字 / 日志
- 队友支援文案与高亮反馈

它不负责定义伤害公式，也不直接写剧情规则。

---

## 十五、当前已知约束 / 风险

### 1. battle engine 中仍有不少 `Math.random()`
用于：

- 闪避
- 暴击
- 随机目标
- 状态命中

这会影响复现性与自动验证稳定性。

### 2. AI 还偏简单
目前没有：

- 多阶段 Boss 逻辑
- 更复杂的资源管理
- 针对状态 / 阵型的智能应对

### 3. 数值仍在持续校准
`createPlayer()` 已回归正式开局模板，快速验证请走 `DebugScreen`；但整体战斗体验仍不等于最终正式平衡。

### 4. 装备 / 更复杂克制尚未落地
这些仍是后续方向，不应写成当前已完成能力。

---

## 十六、当前已落地 vs 后续方向

### 已落地
- ✅ 多对多战斗模型
- ✅ ATB / CTB 行动顺序推进
- ✅ 多目标模式
- ✅ 内功催动外功
- ✅ 状态系统
- ✅ 战斗内道具
- ✅ 队友 / 羁绊支援接入战斗
- ✅ 守回合与保护指定友方的剧情战目标
- ✅ 剧情 / 战斗统一升级与武功熟练度成长闭环
- ✅ 纯函数 battle engine + BattleScreen 编排分层

### 后续可继续深化
- ⏳ 更复杂的敌人 AI
- ⏳ 更明确的装备层
- ⏳ 更复杂的伤害克制与高级联动

---

## 十七、关键文件速查

### 引擎与类型
- `src/game/battle/types.ts`
- `src/game/battle/engine.ts`
- `src/game/battle/adapter.ts`
- `src/game/battle/flow.ts`

### 外层接线
- `src/game/progression.ts`
- `src/game/attributes.ts`
- `src/game/party.ts`
- `src/game/appFlow.ts`
- `src/App.tsx`

### 展示层
- `src/screens/BattleScreen.tsx`

---

## 十八、结论

当前战斗系统已经不再是“能打一架”的试玩模块，而是一套具备以下特征的正式底层：

- 内外分层清楚
- 多单位与行动值模型已成立
- 状态 / 目标模式 / 支援机制都可继续扩展
- 能与剧情系统稳定衔接

它最重要的价值在于：

> **战斗已经从页面功能，升级成了一个可持续迭代的独立系统。**

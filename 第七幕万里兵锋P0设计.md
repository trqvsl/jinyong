# 第七幕“万里兵锋”P0 设计

> 状态：P0 契约定稿；P1-P6 已完成，第七幕七段事件、终局夹具与全路线审计已收口。
> 依据：《射雕英雄传二创剧本大纲.md》A7-01 至 A7-08、现有八幕进度契约、第五幕武穆遗书去向与第六幕幕末状态。
> 目标：用 User Motion -> Use Case -> Interface Mapping 定义统兵、侦察、军纪、平民撤离与抗命流程，并决定是否引入 `CampaignState`。

---

## 一、P0 结论

第七幕不做独立战争模拟器，而做一组有持续后果的脚本化战争事件：

1. 玩家通过剧情节点下军令、分配职责和选择战术。
2. 大军层面的结果由剧情 DSL 结算，玩家只进入决定局部成败的战斗切片。
3. 统兵不是“控制所有单位逐个行动”，而是决定谁去哪里、保护谁、可以承受什么代价。
4. 军纪不是一个单独数值条，而是由具体事件记录：如何处置抢掠、是否给平民留路、是否执行屠城军令。
5. 本幕继续使用 `WorldArcState.variants`，**P0 不引入 `CampaignState`**。
6. 实现前只需补三项最小战斗契约：
   - 剧情战显式声明多个敌人。
   - 保护目标从单人扩展为多人，并支持最低幸存人数。
   - 战斗增加 `partial` 结果，区分无损达成、付出代价后达成和失败。

---

## 二、设计目标与非目标

### 目标

- 让玩家至少三次真正下达影响战场的命令，而不是只听郭靖部署。
- 让侦察结果改变可选战术、敌方编组与伤亡风险。
- 让“打赢”与“守住军纪”成为两套可能冲突的结果。
- 允许战斗目标在有人伤亡时仍部分达成，并让伤亡进入后续文本和状态。
- 让第五幕武穆遗书去向、第六幕余波、大漠成长身份在第七幕有明确读侧。
- 保留成吉思汗、郭靖、华筝、李萍各自不可被玩家代替的核心选择。

### 非目标

- 不做兵员、粮草、士气、城防的长期数值模拟。
- 不做可自由选择多个战区的沙盘地图。
- 不允许玩家逐个控制郭靖、哲别、拖雷或士卒。
- 不新增“统率”属性，不建立与八大根基平行的成长轴。
- 不用一个 `militaryMorality` 数值代替每次具体军纪选择。
- 不在 P0 编写完整事件正文或直接实现第七幕代码。

---

## 三、叙事与玩法边界

### 原著人物保留的核心行动

- 郭靖必须亲自面对蒙古养育之恩与南宋身份的冲突。
- 成吉思汗必须同时保留识人用兵与征服屠杀两面，不能写成单一反派。
- 华筝是否送信、放行或追捕，必须读取她与玩家、郭靖及草原的关系。
- 李萍不会被玩家一句劝说带走；原著死亡是默认结果，存活必须满足完整前置。

### 玩家真正拥有的变量

- 以主帅协力、斥候、粮道管领或独立观察者身份进入西征。
- 决定侦察目标、俘虏处置、兄弟内斗的解决方式和攻城方案。
- 决定是否约束抢掠、是否为平民打开撤离通道。
- 在屠城与南征军令下选择服从、拖延、公开抗命或暗中救人。
- 承担军职、婚约、草原关系、郭靖关系和百姓伤亡的真实代价。

---

## 四、User Motion

### UM-01 草原再召

1. 玩家在主界面看到第七幕目标，前往蒙古大漠。
2. 事件回读 `act6.aftermath`，说明玩家为何此时到场。
3. 军帐回读蒙古态度、苍狼铜符、大漠成长方向和武穆遗书去向。
4. 玩家选择本次西征职责：
   - 随郭靖参与统兵。
   - 领斥候先行。
   - 管粮道、伤兵与后队。
   - 不受军职，只作为说客或独立行动者。
5. 写入 `act7.role`，进入西征大营。

### UM-02 军帐分职与同室操戈

1. 玩家抵达西征大营，先看见军令、伤兵、俘虏和粮车，而不是直接开战。
2. 朮赤、察合台争功，前锋与后队命令冲突。
3. 玩家根据职责选择：
   - 分开两部、重排前后军。
   - 无伤缴械，先保传令与粮道。
   - 借内斗削弱一方，换取自己的指挥权。
   - 拒绝介入，只保住郭靖或关键军报。
4. 写入 `act7.brothers`，决定侦察可用人手与后续援军可靠性。

### UM-03 城外侦察

1. 玩家在撒马尔罕城外查看三个可侦察目标：
   - 城门换防与号令。
   - 水源、粮道与难民通道。
   - 城内主战、求和两派及俘虏口供。
2. 每次只能优先完成一项；大漠成长与既有关系解锁额外方法。
3. 玩家带回可验证情报，不能只靠悟性自动得出最佳答案。
4. 情报质量写入 `act7.intel`，方法写入 `act7.scout-method`。
5. 情报决定城下军议可见的战术、敌人编组和保护对象。

### UM-04 城下军令

1. 玩家在军议中选择攻城方式：
   - 佯攻换防，打开放降通道。
   - 断粮逼降，承受拖延与城内缺粮代价。
   - 强攻城门，以最快速度换取更高伤亡。
   - 先开平民走廊，再发动有限攻势。
2. 选择后进入局部剧情战，不直接模拟整场攻城。
3. 战斗按侦察和军令注入不同 `enemyIds / allyIds / objective`。
4. 结果分为：
   - `won`：目标达成且保护组无人倒下。
   - `partial`：目标达成，但保护组有允许范围内的伤亡。
   - `lost`：目标未达成或幸存人数低于底线。
5. 写入 `act7.siege-plan` 与 `act7.siege-outcome`。

### UM-05 破城军纪与平民撤离

1. 城门失守后，玩家先看到抢掠、报复、伤兵和逃难者，不立即播放胜利庆功。
2. 玩家处理第一起明确军纪冲突：
   - 当众执纪，停下本部抢掠。
   - 只保住当前街区，避免与全军冲突。
   - 默许抢掠，以军功和蒙古信任为先。
   - 借乱暗中组织平民撤离。
3. 若组织撤离，玩家选择保护老弱、守住城门或拖住追兵。
4. 撤离使用多人保护与最低幸存人数，不伪装成“护送”玩法，因为当前战斗没有位置移动。
5. 写入：
   - `act7.discipline`
   - `act7.evacuation`
   - 对应蒙古态度、郭靖关系、karma 与后续 world event

### UM-06 屠城、南征与抗命

1. 成吉思汗下达屠城或以百姓为代价的军令。
2. 玩家必须明确选择：
   - 服从，保住军职和蒙古信任。
   - 争取招降或缓期，自担失败责任。
   - 偷开生路，暗中放走平民。
   - 与郭靖公开抗命。
   - 提前向守城者送信，正式成为蒙古敌人。
3. 西征后，南征宋境的军令把冲突带回李萍与华筝身上。
4. 华筝根据关系和此前行为决定送信、放行、断绝或追捕。
5. 李萍默认以死阻止郭靖屈服；只有满足完整前置，才出现存活分支。
6. 玩家选择南归方式，写入 `act7.departure`。
7. 只有离开草原或明确进入草原长期结局后，才写入 `act7-western-campaign`。

---

## 五、Use Case Enumeration

| ID | 用例 | 前置输入 | 玩家动作 | 可观察结果 | 持久化 |
|---|---|---|---|---|---|
| UC-01 | 确定返草原身份 | `act6.aftermath`、蒙古态度、苍狼铜符 | 接军职、做斥候、管后队、独立行动 | 军帐称呼、可用人手、首个任务不同 | `act7.role` |
| UC-02 | 处理军中内斗 | 郭靖关系、蒙古态度、角色职责 | 调停、缴械、借势、旁观 | 援军可靠性与郭靖评价变化 | `act7.brothers` |
| UC-03 | 选择侦察方向 | 大漠成长、丐帮/宋军/蒙古网络 | 侦察换防、水道、粮道或民情 | 战术选项与敌军编组变化 | `act7.scout-method`、`act7.intel` |
| UC-04 | 处置俘虏 | 军中身份、情报缺口 | 审问、交换、释放、杀俘 | 情报、军纪与阵营态度变化 | 并入 `act7.intel`、`act7.discipline` |
| UC-05 | 下达攻城命令 | 侦察结果、武穆遗书去向 | 佯攻、断粮、强攻、开走廊 | 不同战斗编组、轮数和保护目标 | `act7.siege-plan` |
| UC-06 | 承担攻城伤亡 | 多人保护目标 | 完成局部战斗 | 无损、代价达成或失败三档结果 | `act7.siege-outcome` |
| UC-07 | 执行军纪 | 城破、士卒抢掠 | 执纪、局部约束、默许、暗救 | 军职、军心、百姓与郭靖关系变化 | `act7.discipline` |
| UC-08 | 组织平民撤离 | 路线情报、援军可靠性 | 分配守门、断后、护老弱职责 | 平民完整、部分或撤离失败 | `act7.evacuation` |
| UC-09 | 回应屠城军令 | 军纪与撤离结果 | 服从、拖延、偷放、公开抗命 | 城中死伤、通缉、军职与关系锁定 | `act7.order` |
| UC-10 | 处理华筝立场 | 华筝关系、婚约、抗命状态 | 坦白、求助、拒绝利用、欺骗 | 华筝自主选择放行、断绝或追捕 | `act7.huazheng` |
| UC-11 | 面对李萍终局 | 退路、接应、郭靖抗命、李萍信任 | 提供退路、守出口、接受她断后 | 原著死亡、断后死亡或高门槛存活 | `act7.liping`、NPC 生死 |
| UC-12 | 完成南归 | 屠城、华筝、李萍结果 | 随郭靖、护难民、双面留军、继续军职 | 第八幕入场身份与后续 world event | `act7.departure`、幕级 beat |

---

## 六、前情读侧

| 前情状态 | 第七幕用途 |
|---|---|
| `act6.aftermath=return-damos` | 与郭靖同行到场，直接进入军帐；不额外奖励正确路线 |
| `act6.aftermath=track-wanyan` | 带金蒙情报晚到，可识别金国细作与错误军报 |
| `act6.aftermath=settle-survivors` | 更早接触难民与伤者，平民撤离入口更清楚 |
| `act5.wumu-destination=song-command` | 宋军知道部分阵图；玩家在南征军令下更早被怀疑 |
| `act5.wumu-destination=beggar-network` | 丐帮可提供城内民情、接应与撤离网络 |
| `act5.wumu-destination=player-kept` | 玩家可当面引用军纪原文，但也要承担兵书被用于攻城的责任 |
| `act5.wumu-destination=mongol-copy` | 蒙古军已掌握节要，攻城效率更高；玩家无法假装兵书与征服无关 |
| `shendiao.damos.growth=martial` | 解锁前锋压阵与无伤缴械方案 |
| `shendiao.damos.growth=riding` | 解锁远程斥候、双路传令与追骑误导 |
| `shendiao.damos.growth=survival` | 解锁水源、粮道、伤兵和撤离路线判断 |
| `shendiao.damos.growth=free` | 解锁商队、译者与城内接头方式 |
| `mongol-wolf-tally` | 提供军帐通行与一次合法调兵权，不等于免除违令代价 |
| 蒙古态度、郭靖/华筝/李萍关系 | 改变信任、称呼、求助对象和人物自主选择 |

---

## 七、状态契约

### 阶段状态

以下字段只表示事件段已完成，用于动态路引：

| Key | Value |
|---|---|
| `act7.recall` | `cleared` |
| `act7.camp` | `cleared` |
| `act7.scout` | `cleared` |
| `act7.siege` | `cleared` |
| `act7.city` | `cleared` |
| `act7.home-order` | `cleared` |

### 结果状态

| Key | 建议值 |
|---|---|
| `act7.role` | `command / scout / logistics / independent` |
| `act7.brothers` | `reconciled / disarmed / exploited / unresolved` |
| `act7.scout-method` | `cavalry / supply / civilian / infiltration` |
| `act7.intel` | `complete / partial / exposed` |
| `act7.siege-plan` | `feint / blockade / assault / corridor` |
| `act7.siege-outcome` | `clean / costly / stalled` |
| `act7.discipline` | `enforced / limited / permissive` |
| `act7.evacuation` | `full / partial / failed` |
| `act7.order` | `obeyed / delayed / defied / betrayed` |
| `act7.huazheng` | `helped-escape / broke-ties / stayed-loyal / led-pursuit` |
| `act7.liping` | `died-testimony / died-covering-retreat / survived-prepared` |
| `act7.departure` | `with-guojing / escort-refugees / double-agent / mongol-command / grassland-ending` |

### 幕级完成

- `arcBeat(shendiao/act7-western-campaign=done)` 只在南归或草原长期结局确认后写入。
- 不新增旧样板兼容 beat。
- 第八幕通过 `act7.departure` 决定玩家以叛将、军中旧部、难民护送者、双面人或草原将领身份入场。

---

## 八、李萍存活门槛

默认结果仍是 `died-testimony`。出现 `survived-prepared` 选项必须同时满足：

1. `act7.evacuation=full`，已建立可用安全退路。
2. `act7.order=defied`，郭靖已经明确抗命，不再可能被她的死迫回正确选择。
3. 存在可信接应：
   - `act5.wumu-destination=beggar-network`，或
   - `act5.wumu-destination=song-command`，或
   - `act7.huazheng=helped-escape`。
4. 李萍信任成立：
   - 早年曾救李萍，或
   - 大漠成长选择过 `survival` 并与她维持足够关系。

满足条件也不等于玩家“强行救人”。事件仍由李萍确认郭靖已经作出选择，再决定是否走退路；若出口需要有人断后，她可以进入 `died-covering-retreat`。

---

## 九、事件与地点拆分

### 地点

| locationId | 名称 | 用途 |
|---|---|---|
| `damos` | 蒙古大漠 | 草原再召、华筝、李萍与最终离营 |
| `western-camp` | 西征大营 | 分职、同室操戈、俘虏与军纪前置 |
| `samarkand` | 撒马尔罕 | 城外侦察、攻城、破城军纪与平民撤离 |

### 事件

| 顺序 | eventId | 地点 | 主要职责 |
|---|---|---|---|
| 1 | `shendiao-damos-act7` | `damos` | 回读前情、返草原身份、选择职责 |
| 2 | `shendiao-western-camp-act7` | `western-camp` | 分职、同室操戈、俘虏处理 |
| 3 | `shendiao-samarkand-scout-act7` | `samarkand` | 三类侦察与军议输入 |
| 4 | `shendiao-samarkand-siege-act7` | `samarkand` | 攻城军令与多编组剧情战 |
| 5 | `shendiao-samarkand-aftermath-act7` | `samarkand` | 抢掠军纪、平民撤离、屠城抉择 |
| 6 | `shendiao-damos-home-order-act7` | `damos` | 南征军令、华筝与李萍终局 |
| 7 | `shendiao-damos-departure-act7` | `damos` | 南归、留军或草原长期结局 |

### 动态路引

1. `act6-truth` 完成，且 `act7.recall` 未写入：推荐 `damos`。
2. `act7.recall=cleared`，且 `act7.camp` 未写入：推荐 `western-camp`。
3. `act7.camp=cleared`，且 `act7.scout` 未写入：推荐 `samarkand`，提示城外侦察。
4. `act7.scout=cleared`，且 `act7.siege` 未写入：继续推荐 `samarkand`，提示城下军议。
5. `act7.siege=cleared`，且 `act7.city` 未写入：继续推荐 `samarkand`，提示破城军纪。
6. `act7.city=cleared`，且 `act7.home-order` 未写入：推荐 `damos`，提示南征军令。
7. `act7.home-order=cleared`，且第七幕未完成：继续推荐 `damos`，提示离营与南归。
8. `act7-western-campaign` 完成：推荐切到第八幕华山。

---

## 十、Interface Mapping

### 1. 剧情 DSL

#### 多敌方编组

`Transition.battle` 增加：

```ts
enemyIds?: string[]
```

约束：

- `enemyIds`、`enemyId`、`useLocationPool` 三者只使用一种来源。
- `enemyIds` 按声明顺序建场，允许重复 id 表示同类多单位。
- `StoryCheckpoint.battleEnemyIds` 已能保存实际编组，无需新增存档字段。

#### 多人保护与允许伤亡

在保留旧 `protectAllyId` 兼容的同时增加：

```ts
protectAllyIds?: string[]
minProtectedSurvivors?: number
```

约束：

- 所有 `protectAllyIds` 必须同时出现在 `allyIds`。
- 未提供 `minProtectedSurvivors` 时，默认所有保护目标必须存活。
- 只提供旧 `protectAllyId` 时保持当前“倒下立即失败”行为。
- 当前不增加 `escort` 目标；撤离仍用 `surviveRounds + protectAllyIds` 表达。

#### 部分达成

`Transition.battle` 增加：

```ts
onPartial?: Outcome
```

剧情战结果扩展为：

```ts
type BattleOutcome = "won" | "partial" | "lost" | "fled"
```

判定：

- 主目标完成，保护组全部存活：`won`。
- 主目标完成，保护组有伤亡但仍达到最低幸存人数：`partial`。
- 最低幸存人数已不可能满足，或我方全灭：`lost`。
- 玩家主动脱离：`fled`。

### 2. battle engine

| 文件 | 最小改动 |
|---|---|
| `battle/types.ts` | 运行时保存 `protectUids / minProtectedSurvivors` |
| `battle/engine.ts` | 计算保护组存活数与 `partial` 结果；不足底线时提前失败 |
| `battle/adapter.ts` | 把剧情友方 id 映射为运行时 uid |
| `battle/flow.ts` | 透传四态结果；`partial` 仍结算实际击败敌人的经验和熟练度 |
| `BattleScreen.tsx` | 展示“幸存 N / M”，部分达成使用独立结果文案 |

### 3. app flow 与剧情引擎

| 文件 | 最小改动 |
|---|---|
| `schema.ts` | 增加 `enemyIds`、多人保护字段与 `onPartial` |
| `appFlow.ts` | 按 `enemyIds` 建场；转换多个保护 uid；接受 `partial` |
| `story/engine.ts` | `resolveBattleOutcome()` 将 `partial` 映射到 `onPartial` |
| `App.tsx` | `handleBattleEnd()` 接受四态结果 |
| `verify-story.ts` | 校验敌人来源互斥、保护目标属于友方、最低幸存数范围 |
| `verify-battle.ts` | 覆盖无损、允许伤亡、低于底线、刷新恢复 |

### 4. 数据层

- 新增 `shendiaoAct7.ts`，不继续扩张 `shendiao.ts`。
- `src/data/story/index.ts` 聚合第七幕事件。
- `src/data/map.ts` 新增 `western-camp / samarkand` 并设置事件优先级。
- `src/data/npcs.ts` 至少补齐李萍、华筝、哲别、拖雷、成吉思汗、朮赤、察合台的数据入口。
- `src/data/enemies.ts` 增加守城军、乱兵、追骑等可组合敌方单位。
- `progress.ts` 增加第七幕阶段路引覆盖。
- Debug 预设增加第七幕入口、军纪正线、服从军令、李萍高门槛存活等夹具。

---

## 十一、为什么不引入 CampaignState

### 当前不满足独立模型条件

- 战役是固定七事件链，不存在多战区自由轮转。
- 兵力、粮草、士气不会被玩家长期逐回合经营。
- 统兵选择可以直接映射成事件版本、战斗编组和结果。
- 持久化需求都是离散结论，`arcVariant` 可以清楚表达。
- 新字段会要求 WorldState 迁移、Debug 展示和额外查询 DSL，但当前没有对应收益。

### 重新评估门槛

后续同时出现以下至少两项时，再评估 `CampaignState`：

1. 多个战区可自由选择且会并行变化。
2. 兵员、粮草、士气需要跨五个以上事件持续增减。
3. 玩家可以反复征募、补给、调兵，而不是一次性选择。
4. 其他作品卷也要复用同一套战争循环。
5. 战役中途需要独立于剧情节点保存军团与前线快照。

---

## 十二、验收矩阵

### 结构验收

- 七个事件都能从入口到终点到达。
- 三个地点按 `recall -> camp -> scout/siege/city -> home-order` 顺序露出。
- `act7-western-campaign` 只在离营或草原结局确认后写入。
- 旧华山样板不得在第七幕完成前抢先触发。

### 路线验收

- `command / scout / logistics / independent` 四种职责都能完成本幕。
- 四种武穆遗书去向都至少有两处读侧。
- `martial / riding / survival / free` 四种大漠成长都提供一个独特解法，但不形成唯一正确答案。
- `obeyed / delayed / defied / betrayed` 四种军令结果都有后续代价。
- 李萍存活选项仅在四类门槛全部满足时出现。
- 服从屠城会永久关闭郭靖知己或同行正线，不可只靠后续加好感洗掉。

### 战斗验收

- 显式多敌方编组可刷新恢复。
- 单人保护旧数据保持兼容。
- 多人保护支持：
  - 全员存活 -> `won`
  - 达到最低幸存数 -> `partial`
  - 低于最低幸存数 -> `lost`
- 高速单位、眩晕单位与阵亡单位不会破坏守回合计数。
- `partial` 能进入独立剧情结果，且熟练度和实际击败奖励不丢失。

### 叙事验收

- 每场胜利后都展示伤兵、粮道、难民或报复中的至少一项具体代价。
- 军纪选择必须作用于眼前人物和街区，不只写抽象阵营评价。
- 华筝与李萍保留人物自主选择，不作为玩家路线奖励。
- 南归后的三组“是非善恶”事件通过行动呈现，不替玩家写内心总结。

---

## 十三、后续实施顺序

1. P1（已完成）：实现多敌方、多人保护、最低幸存人数与 `partial` 结果。
2. P2（已完成）：新增地点、NPC/敌人数据、动态路引和第七幕 Debug 入口。
3. P3（已完成）：落地草原再召、军中内斗、俘虏处置与城外侦察。
4. P4（已完成）：落地城下军令、攻城战、破城军纪、平民撤离与屠城抉择。
5. P5（已完成）：落地南征军令、华筝、李萍终局与南归。
6. P6（已完成）：完成路线可达性、伤亡组合、前情读侧、旧事件隔离与浏览器验证。

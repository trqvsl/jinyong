# 内容编写 checklist

> 用于新增 / 改写剧情事件前的快速自检。目标不是替代《剧情系统设计手册》，而是把最容易出错、最常重复的约定压缩成一页。

---

## 1. 先判断：这次写的是什么

动笔前先明确事件类型：

- **地点支线**：补地点气质 / 推进人物关系 / 补主线余波 / 给玩家一个小而真实的选择
- **主线事件**：推进 `arcBeat`，让玩家介入原著因果链
- **world event**：世界对既往行为的回响，不是普通偶遇
- **旧剧情改写**：把小说化叙述改成可接入 `StoryEvent` 的可玩事件

如果这一步说不清，通常说明事件目标还没定。

---

## 2. 先想用途，再写文本

每个事件至少要回答四个问题：

1. 玩家现在在哪？
2. 玩家眼前发生了什么？
3. 玩家为什么要在意？
4. 玩家可以怎么选？

再补一个实现问题：

5. 这个事件完成后，**世界会记住什么**？

如果删掉这个事件，项目会失去什么？

- 一个地点的气质？
- 一段人物关系推进？
- 一条主线因果？
- 一次 world event 回响来源？

答不上来时，先别扩文案。

---

## 3. 事件与节点命名

### event id
- 必须全局唯一
- 用 kebab-case
- 尽量带卷 / 地点 / 主题前缀，避免过泛
  - 推荐：`shendiao-taohua-night-visit`
  - 推荐：`world-beggar-rumor`
  - 不推荐：`event-1`、`story-main`

### node id / nodes key
- `nodes` Record 的 key **必须与 `node.id` 完全一致**
- 同一事件内不要重复 node id
- 虽然当前 `seenNodes` 已改为事件作用域键 ``${eventId}:${nodeId}``，不同事件之间不会再互相污染 `onEnter`，但**仍建议少用过于泛化的 node id**，方便阅读与排查
  - 推荐：`arrival`、`question-rong`、`after-duel`
  - 谨慎：`main`、`next`、`result`

### entryNode
- `entryNode` 必须存在于 `nodes`
- 若事件存在多段流转，先检查所有 `goto` / `gotoEvent` 目标是否真实存在

---

## 4. 结构化字段优先，不要默认写 flag

新增状态前，按这个顺序想：

### 主线推进 / 卷结果
- 用 `arcBeat`
- 卷结局用 `arcEnding`
- 不要再用 `flag` 模拟 `complete` / `ending`

### 人物关系
- 数值亲疏用 `relation`
- 语义关系用 `npcRelationType`
  - 例如：`初识 / 朋友 / 师徒 / 知己 / 恋人`
- 不要用 `flag` 表示“已经熟络”“拜师成功”这类本可结构化的关系语义

### NPC / 阵营状态
- NPC 生死 / 入队 / 阵营 / 命运标签，优先用：
  - `npcAlive`
  - `npcRecruit`
  - `npcFaction`
  - `npcTag`
- 阵营变化优先用：
  - `factionAttitude`
  - `factionPower`

### 一次性触发 / 已完成记录
- `once: true` 事件完成后靠 `completedEvents` 控制不再触发
- world event 的 once 去重靠 `triggeredEvents`
- 不要额外补一层同义 flag

### 只有在没有合适结构化槽位时，才用 `flag`
- 而且必须先确认：后续确实会读它
- 写完后最好顺手问一句：能不能不用 flag？

---

## 5. 事件流转 checklist

### 选择节点
- 选项必须体现**不同立场**，不是同义改写
- `resultText` 要兑现选择，不要只换一句措辞
- 推荐差异：
  - 出手 / 留手 / 不介入
  - 真诚 / 试探 / 敷衍
  - 侠义 / 利己 / 观望

### 纯叙事节点
- 无 `choices` 时，要么：
  - 提供 `autoNext`
  - 要么明确让它成为终点
- 写纯叙事节点时，别忘了 `EventScreen` 有独立的 `autoNext` phase

### battle transition
- 如果用 `battle`，优先补全：
  - `onWin`
  - `onLose`
  - `onFlee`
- 三种结果都要想清楚：
  - 文本怎么收
  - consequences 怎么写
  - 之后 `then` 去哪

### goto / gotoEvent / branch / random
- `goto` 目标节点必须存在
- `gotoEvent` 目标事件必须存在
- `branch` 的默认分支要兜住
- `random` 的候选要有明确用途，不要为了“看起来丰富”而塞随机

---

## 6. 地图挂载与露出顺序

`src/data/map.ts` 里同一地点的 `events` 数组顺序就是优先级：

1. 从前往后找第一个 `notCompleted + condition 通过` 的事件
2. 找不到才回退到通用事件

因此：

- 主线事件应排在通用事件前
- 有明确前置 `arcBeat` 的内容，应排在无 condition 的兜底事件前
- 新增支线后，要检查会不会把原有主线“顶掉”

---

## 7. 文本演出 checklist

### 旁白
- 只写玩家眼前看到的场景、动作、局势变化
- 不替玩家写内心、情绪、价值判断
- 不写未来剧透 / 上帝视角 / 命运论总结

### 对白
- 让对白真正像角色说的话
- 尽量用 `角色名：` 而不是小说腔提示词
  - 推荐：`黄蓉："……"`
  - 不推荐：`黄蓉笑道："……"`
- 一段里若连续多句对白，要交代清楚说话人
- 新人物第一次出现时，必须先给姓名、可见特征或身份关系，不能直接丢出一串陌生人名
- 伪装身份在揭晓前不要映射到本名头像（例如“小叫花”不能提前显示黄蓉人物卡）

### 分页
- 长文本要按 RPG 演出节奏拆拍
- 不要把整段说明一次性砸给玩家
- 也不要机械地一句一页，破坏阅读节奏
- 跨越长时间或进入下一阶段前，优先考虑 `pause` 回据点整备，再从原地点续接
- 一段话不能同时完成多人离场、数个时辰流逝和下一场冲突；日暮、入夜、三更等要拆节点
- 同一日内的时辰变化使用 `sceneTransition`，不要滥用 `consumeDay`

### 场景舞台
- 正文点名且实际在场的人物，应进入 `stage.actors`，不能只活在旁白里
- 同场人物使用稳定 slot；移动、突进、受伤等关键动作才声明 motion
- 追逐、信物、车辆等会改变画面理解的对象，可进入 `stage.props`
- 背景图应留空前景，避免生成背景人物与头像框重复
- 动态道具只挂在动作实际发生的节点；一次性动作不能默认无限循环
- 背景、人物和道具应全部加载完成后整体揭幕，不能让玩家看到逐张补图

### 地点独立空间
- `StoryStage` 是事件节点中的演出舞台；`StoryAreaSpace` 是暂停期可停留、可操作的地点空间，两者不能混用
- 地图点点击后直接进入空间，不增加“选中 → 前往查看”二次确认
- 空间至少声明 `background / description / ambience / residents / actions`
- `storyTargets` 必须同时写 `eventId + nodeId`，不能只靠裸 nodeId 映射续接点
- residents / actions 可用 `visibleDuring(eventId + nodeId)` 处理当前剧情断点显隐；人物生死、阵营等通用 `Condition` 仍未接入空间层
- 驻场人物应提供 `dialogues`；点击人物后，话题选项统一进入空间底部命令框
- 行囊已有独立物品图、分类、详情与场景回流；商店仍复用通用三件货表，未配置地点专属库存前不要承诺独有货物

### 选择与转场
- 只保留会改变立场、资源、关系、路线或有效信息的选项
- 纯观看内容使用 `autoNext`，不要硬塞三种同义回应
- 跨地点必须先落到明确的换场节点，再用 `kind: "travel"` 告诉玩家要去哪里
- 无结果文本的 travel 应直接换场，不插入空结果页
- 选择必须发生在冲突尚未解决时；不能先写人物和解，再让玩家决定如何介入
- 地图点点击后应直接进入独立空间，不再增加“前往查看”二次按钮
- 独立空间至少包含背景、环境说明、驻场人物或可调查痕迹，以及一个真实动作
- 商店、练功、行囊等系统能力应挂在具体地点和人物上，不能只做抽象菜单
- `sceneTransition` 只做展示，不改变 `day`、剧情条件或 checkpoint；需要真实消耗天数仍使用 `consumeDay`

### 现场与对白
- 旁白只写玩家能直接看到的尸身、兵器、脚印、表情和动作
- 追杀缘由、人物来路、谁做了什么，优先让在场人物通过问答推进
- 若旁白已经替所有人物交代完因果，后面的选项通常只剩形式，需要拆成“看见 → 追问 → 回答 → 决定”

### 当前验收口径
- 当前只验收 1280 × 800 桌面端；不因移动端布局额外删减内容或改写节奏
- 历史文档里的移动端验证不作为新内容的完成条件

---

## 8. 信件 / 密帖 / 留书（letter presentation）

如果事件要用 `presentation: "letter"`：

### 外层只写送达过程
- 谁送来
- 在哪里发现
- 信封 / 布包 / 火漆 / 皮绳等外部信息

### 信纸正文只写内容本身
- 正文
- 落款

### 不要把这些塞进信纸正文
- “你回到落脚处时……”
- “信中写道……”
- “字迹如何如何，像那个人一样……”

### 结果文本保持短
- 只交代玩家怎么处理这封信
- 不再补抒情收尾

---

## 9. world event 专项检查

world event 必须能回答：

- 它是由玩家之前做过什么触发的？
- 它体现的是谁 / 哪个阵营 / 哪段江湖风声的回响？
- 为什么它适合进 `pendingWorldEvents`，而不是普通地点事件？

如果只是“在路上偶遇某人聊两句”，那通常不是 world event。

---

## 10. relationType / arcBeat 的常见用法

### relationType
适合表达“关系已经换了语义层级”：

- 初识 → 朋友
- 朋友 → 知己
- 授艺之后 → 师徒

不适合用来替代一切数值亲密度；数值涨跌仍放在 `relation`。

### arcBeat
适合表达“剧情链已经走到哪一步”：

- 某主线节点战胜 / 战败 / 跳过 / 完成
- 后续事件用 `Condition.arcBeat` 读取这一阶段

不要把“拿到一张纸条”“进过一次屋子”这类细小事实都写成 `arcBeat`；那通常更适合普通 consequence 或根本不需要持久化。

---

## 11. 写完后的最小自检

提交前至少过一遍：

- [ ] `event.id` 全局唯一且命名清楚
- [ ] `entryNode` 存在
- [ ] 每个 `nodes` key 都等于对应 `node.id`
- [ ] `goto` / `gotoEvent` 目标真实存在
- [ ] 选项之间有真实立场差异
- [ ] `resultText` 兑现了选择
- [ ] 优先使用了结构化字段，而不是随手加 `flag`
- [ ] 需要书信展示时，送达过程与信纸正文已拆开
- [ ] 地图事件顺序不会抢掉主线 / 前置事件
- [ ] 没有替玩家写内心、未来剧透、小说化总结

如果改动涉及事件结构或条件流转，最后跑：

```bash
npx tsx scripts/verify-story.ts
```

---

## 12. 一句话原则

**先想这个事件对游戏有什么用，再想它写得是否好看。**
